#pragma once

#include <deps/lib_port_structs.h>

PS_HEADER_GLOBAL_START
    PS_HEADER_TYPE_START //struct refcount_struct aka refcount_struct
        PS_HEADER_MEMBER(refs, refs)
    PS_HEADER_TYPE_END(struct refcount_struct, refcount_struct)
    PS_HEADER_TYPE_START //struct kref aka kref
        PS_HEADER_MEMBER(refcount, refcount)
    PS_HEADER_TYPE_END(struct kref, kref)
    PS_HEADER_TYPE_START //struct kobject aka kobject
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(entry, entry)
        PS_HEADER_MEMBER(parent, parent)
        PS_HEADER_MEMBER(kset, kset)
        PS_HEADER_MEMBER(ktype, ktype)
        PS_HEADER_MEMBER(sd, sd)
        PS_HEADER_MEMBER(kref, kref)
    PS_HEADER_TYPE_END(struct kobject, kobject)
    PS_HEADER_TYPE_START //struct resource aka resource
        PS_HEADER_MEMBER(start, start)
        PS_HEADER_MEMBER(end, end)
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(flags, flags)
        PS_HEADER_MEMBER(desc, desc)
        PS_HEADER_MEMBER(sibling, siblings)
        PS_HEADER_MEMBER(child, child)
    PS_HEADER_TYPE_END(struct resource, resource)
    PS_HEADER_TYPE_START //struct vtime aka vtime
        PS_HEADER_MEMBER(seqcount, seqcount)
        PS_HEADER_MEMBER(starttime, starttime)
        PS_HEADER_MEMBER(state, state)
        PS_HEADER_MEMBER(utime, utime)
        PS_HEADER_MEMBER(stime, stime)
        PS_HEADER_MEMBER(gtime, gtime)
    PS_HEADER_TYPE_END(struct vtime, vtime)
    PS_HEADER_TYPE_START //struct prev_cputime aka prev_cputime
    PS_HEADER_TYPE_END(struct prev_cputime, prev_cputime)
    PS_HEADER_TYPE_START //struct load_weight aka load_weight
        PS_HEADER_MEMBER(weight, weight)
        PS_HEADER_MEMBER(inv_weight, inv_weight)
    PS_HEADER_TYPE_END(struct load_weight, load_weight)
    PS_HEADER_TYPE_START //struct sched_avg aka sched_avg
        PS_HEADER_MEMBER(last_update_time, last_update_time)
        PS_HEADER_MEMBER(load_sum, load_sum)
        PS_HEADER_MEMBER(util_sum, util_sum)
        PS_HEADER_MEMBER(period_contrib, period_contrib)
        PS_HEADER_MEMBER(load_avg, load_avg)
        PS_HEADER_MEMBER(util_avg, util_avg)
    PS_HEADER_TYPE_END(struct sched_avg, sched_avg)
    PS_HEADER_TYPE_START //struct sched_statistics aka sched_statistics
        PS_HEADER_MEMBER(wait_start, wait_start)
        PS_HEADER_MEMBER(wait_max, wait_max)
        PS_HEADER_MEMBER(wait_count, wait_count)
        PS_HEADER_MEMBER(wait_sum, wait_sum)
        PS_HEADER_MEMBER(iowait_count, iowait_count)
        PS_HEADER_MEMBER(iowait_sum, iowait_sum)
        PS_HEADER_MEMBER(sleep_start, sleep_start)
        PS_HEADER_MEMBER(sleep_max, sleep_max)
        PS_HEADER_MEMBER(sum_sleep_runtime, sum_sleep_runtime)
        PS_HEADER_MEMBER(block_start, block_start)
        PS_HEADER_MEMBER(block_max, block_max)
        PS_HEADER_MEMBER(exec_max, exec_max)
        PS_HEADER_MEMBER(slice_max, slice_max)
        PS_HEADER_MEMBER(nr_migrations_cold, nr_migrations_cold)
        PS_HEADER_MEMBER(nr_failed_migrations_affine, nr_failed_migrations_affine)
        PS_HEADER_MEMBER(nr_failed_migrations_running, nr_failed_migrations_running)
        PS_HEADER_MEMBER(nr_failed_migrations_hot, nr_failed_migrations_hot)
        PS_HEADER_MEMBER(nr_forced_migrations, nr_forced_migrations)
        PS_HEADER_MEMBER(nr_wakeups, nr_wakeups)
        PS_HEADER_MEMBER(nr_wakeups_sync, nr_wakeups_sync)
        PS_HEADER_MEMBER(nr_wakeups_migrate, nr_wakeups_migrate)
        PS_HEADER_MEMBER(nr_wakeups_local, nr_wakeups_local)
        PS_HEADER_MEMBER(nr_wakeups_remote, nr_wakeups_remote)
        PS_HEADER_MEMBER(nr_wakeups_affine, nr_wakeups_affine)
        PS_HEADER_MEMBER(nr_wakeups_affine_attempts, nr_wakeups_affine_attempts)
        PS_HEADER_MEMBER(nr_wakeups_passive, nr_wakeups_passive)
        PS_HEADER_MEMBER(nr_wakeups_idle, nr_wakeups_idle)
    PS_HEADER_TYPE_END(struct sched_statistics, sched_statistics)
    PS_HEADER_TYPE_START //struct sched_entity aka sched_entity
        PS_HEADER_MEMBER(load, load)
        PS_HEADER_MEMBER(run_node, run_node)
        PS_HEADER_MEMBER(group_node, group_node)
        PS_HEADER_MEMBER(on_rq, on_rq)
        PS_HEADER_MEMBER(exec_start, exec_start)
        PS_HEADER_MEMBER(sum_exec_runtime, sum_exec_runtime)
        PS_HEADER_MEMBER(vruntime, vruntime)
        PS_HEADER_MEMBER(prev_sum_exec_runtime, prev_sum_exec_runtime)
        PS_HEADER_MEMBER(nr_migrations, nr_migrations)
        PS_HEADER_MEMBER(statistics, statistics)
        PS_HEADER_MEMBER(depth, depth)
        PS_HEADER_MEMBER(parent, parent)
        PS_HEADER_MEMBER(cfs_rq, cfs_rq)
        PS_HEADER_MEMBER(my_q, my_q)
        PS_HEADER_MEMBER(avg, avg)
    PS_HEADER_TYPE_END(struct sched_entity, sched_entity)
    PS_HEADER_TYPE_START //struct sched_rt_entity aka sched_rt_entity
        PS_HEADER_MEMBER(run_list, run_list)
        PS_HEADER_MEMBER(timeout, timeout)
        PS_HEADER_MEMBER(watchdog_stamp, watchdog_stamp)
        PS_HEADER_MEMBER(time_slice, time_slice)
        PS_HEADER_MEMBER(on_rq, on_rq)
        PS_HEADER_MEMBER(on_list, on_list)
        PS_HEADER_MEMBER(back, back)
        PS_HEADER_MEMBER(parent, parent)
        PS_HEADER_MEMBER(rt_rq, rt_rq)
        PS_HEADER_MEMBER(my_q, my_q)
    PS_HEADER_TYPE_END(struct sched_rt_entity, sched_rt_entity)
    PS_HEADER_TYPE_START //struct sched_dl_entity aka sched_dl_entity
        PS_HEADER_MEMBER(rb_node, rb_node)
        PS_HEADER_MEMBER(dl_runtime, dl_runtime)
        PS_HEADER_MEMBER(dl_deadline, dl_deadline)
        PS_HEADER_MEMBER(dl_period, dl_period)
        PS_HEADER_MEMBER(dl_bw, dl_bw)
        PS_HEADER_MEMBER(dl_density, dl_density)
        PS_HEADER_MEMBER(runtime, runtime)
        PS_HEADER_MEMBER(deadline, deadline)
        PS_HEADER_MEMBER(flags, flags)
        PS_HEADER_MEMBER(dl_throttled, dl_throttled)
        PS_HEADER_MEMBER(dl_boosted, dl_boosted)
        PS_HEADER_MEMBER(dl_yielded, dl_yielded)
        PS_HEADER_MEMBER(dl_non_contending, dl_non_contending)
        PS_HEADER_MEMBER(dl_timer, dl_timer)
        PS_HEADER_MEMBER(inactive_timer, inactive_timer)
    PS_HEADER_TYPE_END(struct sched_dl_entity, sched_dl_entity)
    PS_HEADER_TYPE_START //struct sd_data aka sd_data
    PS_HEADER_TYPE_END(struct sd_data, sd_data)
    PS_HEADER_TYPE_START //struct sched_domain aka sched_domain
    PS_HEADER_TYPE_END(struct sched_domain, sched_domain)
    PS_HEADER_TYPE_START //struct sched_domain_topology_level aka sched_domain_topology_level
    PS_HEADER_TYPE_END(struct sched_domain_topology_level, sched_domain_topology_level)
    PS_HEADER_TYPE_START //struct sched_domain_shared aka sched_domain_shared
    PS_HEADER_TYPE_END(struct sched_domain_shared, sched_domain_shared)
    PS_HEADER_TYPE_START //struct task_delay_info aka task_delay_info
    PS_HEADER_TYPE_END(struct task_delay_info, task_delay_info)
    PS_HEADER_TYPE_START //struct wake_q_node aka wake_q_node
    PS_HEADER_TYPE_END(struct wake_q_node, wake_q_node)
    PS_HEADER_TYPE_START //struct sighand_struct aka sighand_struct
    PS_HEADER_TYPE_END(struct sighand_struct, sighand_struct)
    PS_HEADER_TYPE_START //struct pacct_struct aka pacct_struct
    PS_HEADER_TYPE_END(struct pacct_struct, pacct_struct)
    PS_HEADER_TYPE_START //struct cpu_itimer aka cpu_itimer
    PS_HEADER_TYPE_END(struct cpu_itimer, cpu_itimer)
    PS_HEADER_TYPE_START //struct task_cputime_atomic aka task_cputime_atomic
    PS_HEADER_TYPE_END(struct task_cputime_atomic, task_cputime_atomic)
    PS_HEADER_TYPE_START //struct thread_group_cputimer aka thread_group_cputimer
    PS_HEADER_TYPE_END(struct thread_group_cputimer, thread_group_cputimer)
    PS_HEADER_TYPE_START //struct signal_struct aka signal_struct
    PS_HEADER_TYPE_END(struct signal_struct, signal_struct)
    PS_HEADER_TYPE_START //struct rw_semaphore aka rw_semaphore
    PS_HEADER_TYPE_END(struct rw_semaphore, rw_semaphore)
    PS_HEADER_TYPE_START //struct spinlock aka spinlock_t
    PS_HEADER_TYPE_END(struct spinlock, spinlock_t)
    PS_HEADER_TYPE_START //struct group_info aka group_info
    PS_HEADER_TYPE_END(struct group_info, group_info)
    PS_HEADER_TYPE_START //struct cred aka cred
    PS_HEADER_TYPE_END(struct cred, cred)
    PS_HEADER_TYPE_START //struct user_struct aka user_struct
        PS_HEADER_MEMBER(__count, __count)
        PS_HEADER_MEMBER(processes, processes)
        PS_HEADER_MEMBER(sigpending, sigpending)
        PS_HEADER_MEMBER(locked_shm, locked_shm)
        PS_HEADER_MEMBER(unix_inflight, unix_inflight)
        PS_HEADER_MEMBER(pipe_bufs, pipe_bufs)
        PS_HEADER_MEMBER(uid_keyring, uid_keyring)
        PS_HEADER_MEMBER(session_keyring, session_keyring)
        PS_HEADER_MEMBER(uidhash_node, uidhash_node)
        PS_HEADER_MEMBER(uid, uid)
        PS_HEADER_MEMBER(locked_vm, locked_vm)
    PS_HEADER_TYPE_END(struct user_struct, user_struct)
    PS_HEADER_TYPE_START //struct key aka key
        PS_HEADER_MEMBER(usage, usage)
        PS_HEADER_MEMBER(serial, serial)
        PS_HEADER_MEMBER(serial_node, serial_node)
        PS_HEADER_MEMBER(sem, sem)
        PS_HEADER_MEMBER(user, user)
        PS_HEADER_MEMBER(security, security)
        PS_HEADER_MEMBER(expiry, expiry)
        PS_HEADER_MEMBER(revoked_at, revoked_at)
        PS_HEADER_MEMBER(last_used_at, last_used_at)
        PS_HEADER_MEMBER(uid, uid)
        PS_HEADER_MEMBER(gid, gid)
        PS_HEADER_MEMBER(perm, perm)
        PS_HEADER_MEMBER(quotalen, quotalen)
        PS_HEADER_MEMBER(datalen, datalen)
        PS_HEADER_MEMBER(state, state)
        PS_HEADER_MEMBER(flags, flags)
    PS_HEADER_TYPE_END(struct key, key)
    PS_HEADER_TYPE_START //struct key_type aka key_type
    PS_HEADER_TYPE_END(struct key_type, key_type)
    PS_HEADER_TYPE_START //struct key_construction aka key_construction
    PS_HEADER_TYPE_END(struct key_construction, key_construction)
    PS_HEADER_TYPE_START //struct key_preparsed_payload aka key_preparsed_payload
    PS_HEADER_TYPE_END(struct key_preparsed_payload, key_preparsed_payload)
    PS_HEADER_TYPE_START //struct key_match_data aka key_match_data
    PS_HEADER_TYPE_END(struct key_match_data, key_match_data)
    PS_HEADER_TYPE_START //struct keyring_index_key aka keyring_index_key
    PS_HEADER_TYPE_END(struct keyring_index_key, keyring_index_key)
    PS_HEADER_TYPE_START //struct task_struct aka task
        PS_HEADER_MEMBER(thread_info, thread_info)
        PS_HEADER_MEMBER(state, state)
        PS_HEADER_MEMBER(stack, stack)
        PS_HEADER_MEMBER(usage, usage)
        PS_HEADER_MEMBER(flags, flags)
        PS_HEADER_MEMBER(ptrace, ptrace)
        PS_HEADER_MEMBER(wake_entry, wake_entry)
        PS_HEADER_MEMBER(on_cpu, on_cpu)
        PS_HEADER_MEMBER(cpu, cpu)
        PS_HEADER_MEMBER(wakee_flips, wakee_flips)
        PS_HEADER_MEMBER(wakee_flip_decay_ts, wakee_flip_decay_ts)
        PS_HEADER_MEMBER(last_wakee, last_wakee)
        PS_HEADER_MEMBER(wake_cpu, wake_cpu)
        PS_HEADER_MEMBER(on_rq, on_rq)
        PS_HEADER_MEMBER(prio, prio)
        PS_HEADER_MEMBER(static_prio, static_prio)
        PS_HEADER_MEMBER(normal_prio, normal_prio)
        PS_HEADER_MEMBER(rt_priority, rt_priority)
        PS_HEADER_MEMBER(sched_class, sched_class)
        PS_HEADER_MEMBER(se, se)
        PS_HEADER_MEMBER(rt, rt)
        PS_HEADER_MEMBER(sched_task_group, sched_task_group)
        PS_HEADER_MEMBER(dl, dl)
        PS_HEADER_MEMBER(preempt_notifiers, preempt_notifiers)
        PS_HEADER_MEMBER(btrace_seq, btrace_seq)
        PS_HEADER_MEMBER(policy, policy)
        PS_HEADER_MEMBER(nr_cpus_allowed, nr_cpus_allowed)
        PS_HEADER_MEMBER(cpus_allowed, cpus_allowed)
        PS_HEADER_MEMBER(sched_info, sched_info)
        PS_HEADER_MEMBER(tasks, tasks)
        PS_HEADER_MEMBER(pushable_tasks, pushable_tasks)
        PS_HEADER_MEMBER(pushable_dl_tasks, pushable_dl_tasks)
        PS_HEADER_MEMBER(mm, mm)
        PS_HEADER_MEMBER(active_mm, active_mm)
        PS_HEADER_MEMBER(vmacache, vmacache)
        PS_HEADER_MEMBER(rss_stat, rss_stat)
        PS_HEADER_MEMBER(exit_state, exit_state)
        PS_HEADER_MEMBER(exit_code, exit_code)
        PS_HEADER_MEMBER(exit_signal, exit_signal)
        PS_HEADER_MEMBER(pdeath_signal, pdeath_signal)
        PS_HEADER_MEMBER(jobctl, jobctl)
        PS_HEADER_MEMBER(personality, personality)
        PS_HEADER_MEMBER(atomic_flags, atomic_flags)
        PS_HEADER_MEMBER(restart_block, restart_block)
        PS_HEADER_MEMBER(pid, pid)
        PS_HEADER_MEMBER(tgid, tgid)
        PS_HEADER_MEMBER(real_parent, real_parent)
        PS_HEADER_MEMBER(parent, parent)
        PS_HEADER_MEMBER(children, children)
        PS_HEADER_MEMBER(sibling, sibling)
        PS_HEADER_MEMBER(group_leader, group_leader)
        PS_HEADER_MEMBER(ptraced, ptraced)
        PS_HEADER_MEMBER(ptrace_entry, ptrace_entry)
        PS_HEADER_MEMBER(pids, pids)
        PS_HEADER_MEMBER(thread_group, thread_group)
        PS_HEADER_MEMBER(thread_node, thread_node)
        PS_HEADER_MEMBER(vfork_done, vfork_done)
        PS_HEADER_MEMBER(set_child_tid, set_child_tid)
        PS_HEADER_MEMBER(clear_child_tid, clear_child_tid)
        PS_HEADER_MEMBER(utime, utime)
        PS_HEADER_MEMBER(stime, stime)
        PS_HEADER_MEMBER(gtime, gtime)
        PS_HEADER_MEMBER(prev_cputime, prev_cputime)
        PS_HEADER_MEMBER(vtime, vtime)
        PS_HEADER_MEMBER(tick_dep_mask, tick_dep_mask)
        PS_HEADER_MEMBER(nvcsw, nvcsw)
        PS_HEADER_MEMBER(nivcsw, nivcsw)
        PS_HEADER_MEMBER(start_time, start_time)
        PS_HEADER_MEMBER(real_start_time, real_start_time)
        PS_HEADER_MEMBER(min_flt, min_flt)
        PS_HEADER_MEMBER(maj_flt, maj_flt)
        PS_HEADER_MEMBER(cputime_expires, cputime_expires)
        PS_HEADER_MEMBER(cpu_timers, cpu_timers)
        PS_HEADER_MEMBER(ptracer_cred, ptracer_cred)
        PS_HEADER_MEMBER(real_cred, real_cred)
        PS_HEADER_MEMBER(cred, cred)
        PS_HEADER_MEMBER(comm, comm)
        PS_HEADER_MEMBER(nameidata, nameidata)
        PS_HEADER_MEMBER(sysvsem, sysvsem)
        PS_HEADER_MEMBER(sysvshm, sysvshm)
        PS_HEADER_MEMBER(last_switch_count, last_switch_count)
        PS_HEADER_MEMBER(fs, fs)
        PS_HEADER_MEMBER(files, files)
        PS_HEADER_MEMBER(nsproxy, nsproxy)
        PS_HEADER_MEMBER(signal, signal)
        PS_HEADER_MEMBER(sighand, sighand)
        PS_HEADER_MEMBER(blocked, blocked)
        PS_HEADER_MEMBER(real_blocked, real_blocked)
        PS_HEADER_MEMBER(saved_sigmask, saved_sigmask)
        PS_HEADER_MEMBER(pending, pending)
        PS_HEADER_MEMBER(sas_ss_sp, sas_ss_sp)
        PS_HEADER_MEMBER(sas_ss_size, sas_ss_size)
        PS_HEADER_MEMBER(sas_ss_flags, sas_ss_flags)
        PS_HEADER_MEMBER(thread, thread)
    PS_HEADER_TYPE_END(struct task_struct, task)
    PS_HEADER_TYPE_START //struct fwnode_handle aka fwnode_handle
    PS_HEADER_TYPE_END(struct fwnode_handle, fwnode_handle)
    PS_HEADER_TYPE_START //struct thread_info aka thread_info
    PS_HEADER_TYPE_END(struct thread_info, thread_info)
    PS_HEADER_TYPE_START //struct thread_struct aka thread_struct
    PS_HEADER_TYPE_END(struct thread_struct, thread_struct)
    PS_HEADER_TYPE_START //struct upid aka upid
        PS_HEADER_MEMBER(nr, nr)
        PS_HEADER_MEMBER(ns, ns)
        PS_HEADER_MEMBER(pid_chain, pid_chain)
    PS_HEADER_TYPE_END(struct upid, upid)
    PS_HEADER_TYPE_START //struct pid aka pid
        PS_HEADER_MEMBER(count, count)
        PS_HEADER_MEMBER(level, level)
        PS_HEADER_MEMBER(tasks, tasks)
        PS_HEADER_MEMBER(rcu, rcu)
        PS_HEADER_MEMBER(numbers, numbers)
    PS_HEADER_TYPE_END(struct pid, pid)
    PS_HEADER_TYPE_START //struct pid_link aka pid_link
        PS_HEADER_MEMBER(node, node)
        PS_HEADER_MEMBER(pid, pid)
    PS_HEADER_TYPE_END(struct pid_link, pid_link)
    PS_HEADER_TYPE_START //struct pid_namespace aka pid_namespace
    PS_HEADER_TYPE_END(struct pid_namespace, pid_namespace)
    PS_HEADER_TYPE_START //struct pidmap aka pidmap
    PS_HEADER_TYPE_END(struct pidmap, pidmap)
    PS_HEADER_TYPE_START //struct mutex aka mutex
    PS_HEADER_TYPE_END(struct mutex, mutex)
    PS_HEADER_TYPE_START //struct mutex_waiter aka mutex_waiter
    PS_HEADER_TYPE_END(struct mutex_waiter, mutex_waiter)
    PS_HEADER_TYPE_START //struct kstat aka kstat
        PS_HEADER_MEMBER(result_mask, result_mask)
        PS_HEADER_MEMBER(mode, mode)
        PS_HEADER_MEMBER(nlink, nlink)
        PS_HEADER_MEMBER(blksize, blksize)
        PS_HEADER_MEMBER(attributes, attributes)
        PS_HEADER_MEMBER(attributes_mask, attributes_mask)
        PS_HEADER_MEMBER(ino, ino)
        PS_HEADER_MEMBER(dev, dev)
        PS_HEADER_MEMBER(rdev, rdev)
        PS_HEADER_MEMBER(uid, uid)
        PS_HEADER_MEMBER(gid, gid)
        PS_HEADER_MEMBER(size, size)
        PS_HEADER_MEMBER(atime, atime)
        PS_HEADER_MEMBER(mtime, mtime)
        PS_HEADER_MEMBER(ctime, ctime)
        PS_HEADER_MEMBER(btime, btime)
        PS_HEADER_MEMBER(blocks, blocks)
    PS_HEADER_TYPE_END(struct kstat, kstat)
    PS_HEADER_TYPE_START //struct kstatfs aka kstatfs
        PS_HEADER_MEMBER(f_type, f_type)
        PS_HEADER_MEMBER(f_bsize, f_bsize)
        PS_HEADER_MEMBER(f_blocks, f_blocks)
        PS_HEADER_MEMBER(f_bfree, f_bfree)
        PS_HEADER_MEMBER(f_bavail, f_bavail)
        PS_HEADER_MEMBER(f_files, f_files)
        PS_HEADER_MEMBER(f_ffree, f_ffree)
        PS_HEADER_MEMBER(f_fsid, f_fsid)
        PS_HEADER_MEMBER(f_namelen, f_namelen)
        PS_HEADER_MEMBER(f_frsize, f_frsize)
        PS_HEADER_MEMBER(f_flags, f_flags)
        PS_HEADER_MEMBER(f_spare, f_spare)
    PS_HEADER_TYPE_END(struct kstatfs, kstatfs)
    PS_HEADER_TYPE_START //struct filename aka filename
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(uptr, uptr)
        PS_HEADER_MEMBER(aname, aname)
        PS_HEADER_MEMBER(refcnt, refcnt)
    PS_HEADER_TYPE_END(struct filename, filename)
    PS_HEADER_TYPE_START //struct file aka file
        PS_HEADER_MEMBER(f_u.fu_llist, f_u_fu_llist)
        PS_HEADER_MEMBER(f_u.fu_rcuhead, f_u_fu_rcuhead)
        PS_HEADER_MEMBER(f_path, f_path)
        PS_HEADER_MEMBER(f_inode, f_inode)
        PS_HEADER_MEMBER(f_op, f_op)
        PS_HEADER_MEMBER(f_lock, f_lock)
        PS_HEADER_MEMBER(f_write_hint, f_write_hint)
        PS_HEADER_MEMBER(f_count, f_count)
        PS_HEADER_MEMBER(f_flags, f_flags)
        PS_HEADER_MEMBER(f_mode, f_mode)
        PS_HEADER_MEMBER(f_pos_lock, f_pos_lock)
        PS_HEADER_MEMBER(f_pos, f_pos)
        PS_HEADER_MEMBER(f_owner, f_owner)
        PS_HEADER_MEMBER(f_cred, f_cred)
        PS_HEADER_MEMBER(f_ra, f_ra)
        PS_HEADER_MEMBER(f_version, f_version)
        PS_HEADER_MEMBER(f_security, f_security)
        PS_HEADER_MEMBER(private_data, private_data)
        PS_HEADER_MEMBER(f_mapping, f_mapping)
        PS_HEADER_MEMBER(f_wb_err, f_wb_err)
    PS_HEADER_TYPE_END(struct file, file)
    PS_HEADER_TYPE_START //struct path aka path
        PS_HEADER_MEMBER(mnt, mnt)
        PS_HEADER_MEMBER(dentry, dentry)
    PS_HEADER_TYPE_END(struct path, path)
    PS_HEADER_TYPE_START //struct inode aka inode
        PS_HEADER_MEMBER(i_mode, i_mode)
        PS_HEADER_MEMBER(i_opflags, i_opflags)
        PS_HEADER_MEMBER(i_uid, i_uid)
        PS_HEADER_MEMBER(i_gid, i_gid)
        PS_HEADER_MEMBER(i_flags, i_flags)
        PS_HEADER_MEMBER(i_op, i_op)
        PS_HEADER_MEMBER(i_sb, i_sb)
        PS_HEADER_MEMBER(i_mapping, i_mapping)
        PS_HEADER_MEMBER(i_ino, i_ino)
        PS_HEADER_MEMBER(i_nlink, i_nlink)
        PS_HEADER_MEMBER(i_rdev, i_rdev)
        PS_HEADER_MEMBER(i_size, i_size)
        PS_HEADER_MEMBER(i_atime, i_atime)
        PS_HEADER_MEMBER(i_mtime, i_mtime)
        PS_HEADER_MEMBER(i_ctime, i_ctime)
        PS_HEADER_MEMBER(i_lock, i_lock)
        PS_HEADER_MEMBER(i_bytes, i_bytes)
        PS_HEADER_MEMBER(i_blkbits, i_blkbits)
        PS_HEADER_MEMBER(i_write_hint, i_write_hint)
        PS_HEADER_MEMBER(i_blocks, i_blocks)
        PS_HEADER_MEMBER(i_state, i_state)
        PS_HEADER_MEMBER(i_rwsem, i_rwsem)
        PS_HEADER_MEMBER(dirtied_when, dirtied_when)
        PS_HEADER_MEMBER(dirtied_time_when, dirtied_time_when)
        PS_HEADER_MEMBER(i_hash, i_hash)
        PS_HEADER_MEMBER(i_io_list, i_io_list)
        PS_HEADER_MEMBER(i_lru, i_lru)
        PS_HEADER_MEMBER(i_sb_list, i_sb_list)
        PS_HEADER_MEMBER(i_wb_list, i_wb_list)
        PS_HEADER_MEMBER(i_dentry, i_dentry)
        PS_HEADER_MEMBER(i_rcu, i_rcu)
        PS_HEADER_MEMBER(i_version, i_version)
        PS_HEADER_MEMBER(i_count, i_count)
        PS_HEADER_MEMBER(i_dio_count, i_dio_count)
        PS_HEADER_MEMBER(i_writecount, i_writecount)
        PS_HEADER_MEMBER(i_fop, i_fop)
        PS_HEADER_MEMBER(i_flctx, i_flctx)
        PS_HEADER_MEMBER(i_data, i_data)
        PS_HEADER_MEMBER(i_devices, i_devices)
        PS_HEADER_MEMBER(i_pipe, i_pipe)
        PS_HEADER_MEMBER(i_bdev, i_bdev)
        PS_HEADER_MEMBER(i_cdev, i_cdev)
        PS_HEADER_MEMBER(i_link, i_link)
        PS_HEADER_MEMBER(i_dir_seq, i_dir_seq)
        PS_HEADER_MEMBER(i_generation, i_generation)
        PS_HEADER_MEMBER(i_private, i_private)
    PS_HEADER_TYPE_END(struct inode, inode)
    PS_HEADER_TYPE_START //struct iattr aka iattr
        PS_HEADER_MEMBER(ia_valid, ia_valid)
        PS_HEADER_MEMBER(ia_mode, ia_mode)
        PS_HEADER_MEMBER(ia_uid, ia_uid)
        PS_HEADER_MEMBER(ia_gid, ia_gid)
        PS_HEADER_MEMBER(ia_size, ia_size)
        PS_HEADER_MEMBER(ia_atime, ia_atime)
        PS_HEADER_MEMBER(ia_mtime, ia_mtime)
        PS_HEADER_MEMBER(ia_ctime, ia_ctime)
        PS_HEADER_MEMBER(ia_file, ia_file)
    PS_HEADER_TYPE_END(struct iattr, iattr)
    PS_HEADER_TYPE_START //struct block_device aka block_device
        PS_HEADER_MEMBER(bd_dev, bd_dev)
        PS_HEADER_MEMBER(bd_openers, bd_openers)
        PS_HEADER_MEMBER(bd_inode, bd_inode)
        PS_HEADER_MEMBER(bd_super, bd_super)
        PS_HEADER_MEMBER(bd_mutex, bd_mutex)
        PS_HEADER_MEMBER(bd_claiming, bd_claiming)
        PS_HEADER_MEMBER(bd_holder, bd_holder)
        PS_HEADER_MEMBER(bd_holders, bd_holders)
        PS_HEADER_MEMBER(bd_write_holder, bd_write_holder)
        PS_HEADER_MEMBER(bd_holder_disks, bd_holder_disks)
        PS_HEADER_MEMBER(bd_contains, bd_contains)
        PS_HEADER_MEMBER(bd_block_size, bd_block_size)
        PS_HEADER_MEMBER(bd_partno, bd_partno)
        PS_HEADER_MEMBER(bd_part, bd_part)
        PS_HEADER_MEMBER(bd_part_count, bd_part_count)
        PS_HEADER_MEMBER(bd_invalidated, bd_invalidated)
        PS_HEADER_MEMBER(bd_disk, bd_disk)
        PS_HEADER_MEMBER(bd_queue, bd_queue)
        PS_HEADER_MEMBER(bd_bdi, bd_bdi)
        PS_HEADER_MEMBER(bd_list, bd_list)
        PS_HEADER_MEMBER(bd_private, bd_private)
        PS_HEADER_MEMBER(bd_fsfreeze_count, bd_fsfreeze_count)
        PS_HEADER_MEMBER(bd_fsfreeze_mutex, bd_fsfreeze_mutex)
    PS_HEADER_TYPE_END(struct block_device, block_device)
    PS_HEADER_TYPE_START //struct fown_struct aka fown_struct
        PS_HEADER_MEMBER(lock, lock)
        PS_HEADER_MEMBER(pid, pid)
        PS_HEADER_MEMBER(pid_type, pid_type)
        PS_HEADER_MEMBER(uid, uid)
        PS_HEADER_MEMBER(euid, euid)
        PS_HEADER_MEMBER(signum, signum)
    PS_HEADER_TYPE_END(struct fown_struct, fown_struct)
    PS_HEADER_TYPE_START //struct fasync_struct aka fasync_struct 
    PS_HEADER_TYPE_END(struct fasync_struct, fasync_struct )
    PS_HEADER_TYPE_START //struct sb_writers aka sb_writers
    PS_HEADER_TYPE_END(struct sb_writers, sb_writers)
    PS_HEADER_TYPE_START //struct super_block aka super_block
    PS_HEADER_TYPE_END(struct super_block, super_block)
    PS_HEADER_TYPE_START //struct dir_context aka dir_context
        PS_HEADER_MEMBER(actor, actor)
        PS_HEADER_MEMBER(pos, pos)
    PS_HEADER_TYPE_END(struct dir_context, dir_context)
    PS_HEADER_TYPE_START //struct file_system_type aka file_system_type
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(mount, mount)
        PS_HEADER_MEMBER(fs_flags, fs_flags)
        PS_HEADER_MEMBER(kill_sb, kill_sb)
        PS_HEADER_MEMBER(owner, owner)
        PS_HEADER_MEMBER(next, next)
        PS_HEADER_MEMBER(fs_supers, fs_supers)
        PS_HEADER_MEMBER(s_lock_key, s_lock_key)
        PS_HEADER_MEMBER(s_umount_key, s_umount_key)
        PS_HEADER_MEMBER(s_vfs_rename_key, s_vfs_rename_key)
        PS_HEADER_MEMBER(s_writers_key, s_writers_key)
        PS_HEADER_MEMBER(i_lock_key, i_lock_key)
        PS_HEADER_MEMBER(i_mutex_key, i_mutex_key)
        PS_HEADER_MEMBER(i_mutex_dir_key, i_mutex_dir_key)
    PS_HEADER_TYPE_END(struct file_system_type, file_system_type)
    PS_HEADER_TYPE_START //struct vfsmount aka vfsmount
        PS_HEADER_MEMBER(mnt_root, mnt_root)
        PS_HEADER_MEMBER(mnt_sb, mnt_sb)
        PS_HEADER_MEMBER(mnt_flags, mnt_flags)
    PS_HEADER_TYPE_END(struct vfsmount, vfsmount)
    PS_HEADER_TYPE_START //struct fs_struct aka fs_struct
        PS_HEADER_MEMBER(users, users)
        PS_HEADER_MEMBER(lock, lock)
        PS_HEADER_MEMBER(seq, seq)
        PS_HEADER_MEMBER(umask, umask)
        PS_HEADER_MEMBER(in_exec, in_exec)
        PS_HEADER_MEMBER(root, root)
        PS_HEADER_MEMBER(pwd, pwd)
    PS_HEADER_TYPE_END(struct fs_struct, fs_struct)
    PS_HEADER_TYPE_START //struct file_ra_state aka file_ra_state
        PS_HEADER_MEMBER(start, start)
        PS_HEADER_MEMBER(size, size)
        PS_HEADER_MEMBER(async_size, async_size)
        PS_HEADER_MEMBER(ra_pages, ra_pages)
        PS_HEADER_MEMBER(mmap_miss, mmap_miss)
        PS_HEADER_MEMBER(prev_pos, prev_pos)
    PS_HEADER_TYPE_END(struct file_ra_state, file_ra_state)
    PS_HEADER_TYPE_START //struct file_lock aka file_lock
        PS_HEADER_MEMBER(fl_next, fl_next)
        PS_HEADER_MEMBER(fl_list, fl_list)
        PS_HEADER_MEMBER(fl_link, fl_link)
        PS_HEADER_MEMBER(fl_block, fl_block)
        PS_HEADER_MEMBER(fl_owner, fl_owner)
        PS_HEADER_MEMBER(fl_flags, fl_flags)
        PS_HEADER_MEMBER(fl_type, fl_type)
        PS_HEADER_MEMBER(fl_pid, fl_pid)
        PS_HEADER_MEMBER(fl_link_cpu, fl_link_cpu)
        PS_HEADER_MEMBER(fl_wait, fl_wait)
        PS_HEADER_MEMBER(fl_file, fl_file)
        PS_HEADER_MEMBER(fl_start, fl_start)
        PS_HEADER_MEMBER(fl_end, fl_end)
        PS_HEADER_MEMBER(fl_fasync, fl_fasync)
        PS_HEADER_MEMBER(fl_break_time, fl_break_time)
        PS_HEADER_MEMBER(fl_downgrade_time, fl_downgrade_time)
        PS_HEADER_MEMBER(fl_ops, fl_ops)
        PS_HEADER_MEMBER(fl_lmops, fl_lmops)
    PS_HEADER_TYPE_END(struct file_lock, file_lock)
    PS_HEADER_TYPE_START //struct file_lock_operations aka file_lock_operations
        PS_HEADER_MEMBER(fl_release_private, fl_release_private)
    PS_HEADER_TYPE_END(struct file_lock_operations, file_lock_operations)
    PS_HEADER_TYPE_START //struct lock_manager aka lock_manager
        PS_HEADER_MEMBER(list, list)
        PS_HEADER_MEMBER(block_opens, block_opens)
    PS_HEADER_TYPE_END(struct lock_manager, lock_manager)
    PS_HEADER_TYPE_START //struct lock_manager_operations aka lock_manager_operations
        PS_HEADER_MEMBER(lm_compare_owner, lm_compare_owner)
        PS_HEADER_MEMBER(lm_owner_key, lm_owner_key)
        PS_HEADER_MEMBER(lm_get_owner, lm_get_owner)
        PS_HEADER_MEMBER(lm_put_owner, lm_put_owner)
        PS_HEADER_MEMBER(lm_notify, lm_notify)
        PS_HEADER_MEMBER(lm_grant, lm_grant)
        PS_HEADER_MEMBER(lm_break, lm_break)
        PS_HEADER_MEMBER(lm_change, lm_change)
        PS_HEADER_MEMBER(lm_setup, lm_setup)
    PS_HEADER_TYPE_END(struct lock_manager_operations, lock_manager_operations)
    PS_HEADER_TYPE_START //struct file_operations aka file_operations
        PS_HEADER_MEMBER(owner, owner)
        PS_HEADER_MEMBER(llseek, llseek)
        PS_HEADER_MEMBER(read, read)
        PS_HEADER_MEMBER(write, write)
        PS_HEADER_MEMBER(read_iter, read_iter)
        PS_HEADER_MEMBER(write_iter, write_iter)
        PS_HEADER_MEMBER(iterate, iterate)
        PS_HEADER_MEMBER(iterate_shared, iterate_shared)
        PS_HEADER_MEMBER(poll, poll)
        PS_HEADER_MEMBER(unlocked_ioctl, unlocked_ioctl)
        PS_HEADER_MEMBER(compat_ioctl, compat_ioctl)
        PS_HEADER_MEMBER(mmap, mmap)
        PS_HEADER_MEMBER(open, open)
        PS_HEADER_MEMBER(flush, flush)
        PS_HEADER_MEMBER(release, release)
        PS_HEADER_MEMBER(fsync, fsync)
        PS_HEADER_MEMBER(fasync, fasync)
        PS_HEADER_MEMBER(lock, lock)
        PS_HEADER_MEMBER(sendpage, sendpage)
        PS_HEADER_MEMBER(get_unmapped_area, get_unmapped_area)
        PS_HEADER_MEMBER(check_flags, check_flags)
        PS_HEADER_MEMBER(flock, flock)
        PS_HEADER_MEMBER(splice_write, splice_write)
        PS_HEADER_MEMBER(splice_read, splice_read)
        PS_HEADER_MEMBER(setlease, setlease)
        PS_HEADER_MEMBER(fallocate, fallocate)
        PS_HEADER_MEMBER(show_fdinfo, show_fdinfo)
        PS_HEADER_MEMBER(copy_file_range, copy_file_range)
        PS_HEADER_MEMBER(clone_file_range, clone_file_range)
        PS_HEADER_MEMBER(dedupe_file_range, dedupe_file_range)
    PS_HEADER_TYPE_END(struct file_operations, file_operations)
    PS_HEADER_TYPE_START //struct inode_operations aka inode_operations
        PS_HEADER_MEMBER(lookup, lookup)
        PS_HEADER_MEMBER(get_link, get_link)
        PS_HEADER_MEMBER(permission, permission)
        PS_HEADER_MEMBER(get_acl, get_acl)
        PS_HEADER_MEMBER(readlink, readlink)
        PS_HEADER_MEMBER(create, create)
        PS_HEADER_MEMBER(link, link)
        PS_HEADER_MEMBER(unlink, unlink)
        PS_HEADER_MEMBER(symlink, symlink)
        PS_HEADER_MEMBER(mkdir, mkdir)
        PS_HEADER_MEMBER(rmdir, rmdir)
        PS_HEADER_MEMBER(mknod, mknod)
        PS_HEADER_MEMBER(rename, rename)
        PS_HEADER_MEMBER(setattr, setattr)
        PS_HEADER_MEMBER(getattr, getattr)
        PS_HEADER_MEMBER(listxattr, listxattr)
        PS_HEADER_MEMBER(fiemap, fiemap)
        PS_HEADER_MEMBER(update_time, update_time)
        PS_HEADER_MEMBER(atomic_open, atomic_open)
        PS_HEADER_MEMBER(tmpfile, tmpfile)
        PS_HEADER_MEMBER(set_acl, set_acl)
    PS_HEADER_TYPE_END(struct inode_operations, inode_operations)
    PS_HEADER_TYPE_START //struct super_operations aka super_operations
        PS_HEADER_MEMBER(alloc_inode, alloc_inode)
        PS_HEADER_MEMBER(destroy_inode, destroy_inode)
        PS_HEADER_MEMBER(dirty_inode, dirty_inode)
        PS_HEADER_MEMBER(write_inode, write_inode)
        PS_HEADER_MEMBER(drop_inode, drop_inode)
        PS_HEADER_MEMBER(evict_inode, evict_inode)
        PS_HEADER_MEMBER(put_super, put_super)
        PS_HEADER_MEMBER(sync_fs, sync_fs)
        PS_HEADER_MEMBER(freeze_super, freeze_super)
        PS_HEADER_MEMBER(freeze_fs, freeze_fs)
        PS_HEADER_MEMBER(thaw_super, thaw_super)
        PS_HEADER_MEMBER(unfreeze_fs, unfreeze_fs)
        PS_HEADER_MEMBER(statfs, statfs)
        PS_HEADER_MEMBER(remount_fs, remount_fs)
        PS_HEADER_MEMBER(umount_begin, umount_begin)
        PS_HEADER_MEMBER(show_options, show_options)
        PS_HEADER_MEMBER(show_devname, show_devname)
        PS_HEADER_MEMBER(show_path, show_path)
        PS_HEADER_MEMBER(show_stats, show_stats)
        PS_HEADER_MEMBER(nr_cached_objects, nr_cached_objects)
        PS_HEADER_MEMBER(free_cached_objects, free_cached_objects)
    PS_HEADER_TYPE_END(struct super_operations, super_operations)
    PS_HEADER_TYPE_START //struct address_space aka address_space
        PS_HEADER_MEMBER(host, host)
        PS_HEADER_MEMBER(page_tree, page_tree)
        PS_HEADER_MEMBER(tree_lock, tree_lock)
        PS_HEADER_MEMBER(i_mmap_writable, i_mmap_writable)
        PS_HEADER_MEMBER(i_mmap, i_mmap)
        PS_HEADER_MEMBER(i_mmap_rwsem, i_mmap_rwsem)
        PS_HEADER_MEMBER(nrpages, nrpages)
        PS_HEADER_MEMBER(nrexceptional, nrexceptional)
        PS_HEADER_MEMBER(a_ops, a_ops)
        PS_HEADER_MEMBER(flags, flags)
        PS_HEADER_MEMBER(private_lock, private_lock)
        PS_HEADER_MEMBER(gfp_mask, gfp_mask)
        PS_HEADER_MEMBER(private_list, private_list)
        PS_HEADER_MEMBER(private_data, private_data)
        PS_HEADER_MEMBER(wb_err, wb_err)
    PS_HEADER_TYPE_END(struct address_space, address_space)
    PS_HEADER_TYPE_START //struct address_space_operations aka address_space_operations
        PS_HEADER_MEMBER(writepage, writepage)
        PS_HEADER_MEMBER(readpage, readpage)
        PS_HEADER_MEMBER(writepages, writepages)
        PS_HEADER_MEMBER(set_page_dirty, set_page_dirty)
        PS_HEADER_MEMBER(readpages, readpages)
        PS_HEADER_MEMBER(write_begin, write_begin)
        PS_HEADER_MEMBER(write_end, write_end)
        PS_HEADER_MEMBER(bmap, bmap)
        PS_HEADER_MEMBER(invalidatepage, invalidatepage)
        PS_HEADER_MEMBER(releasepage, releasepage)
        PS_HEADER_MEMBER(freepage, freepage)
        PS_HEADER_MEMBER(direct_IO, direct_IO)
        PS_HEADER_MEMBER(migratepage, migratepage)
        PS_HEADER_MEMBER(isolate_page, isolate_page)
        PS_HEADER_MEMBER(putback_page, putback_page)
        PS_HEADER_MEMBER(launder_page, launder_page)
        PS_HEADER_MEMBER(is_partially_uptodate, is_partially_uptodate)
        PS_HEADER_MEMBER(is_dirty_writeback, is_dirty_writeback)
        PS_HEADER_MEMBER(error_remove_page, error_remove_page)
        PS_HEADER_MEMBER(swap_activate, swap_activate)
        PS_HEADER_MEMBER(swap_deactivate, swap_deactivate)
    PS_HEADER_TYPE_END(struct address_space_operations, address_space_operations)
    PS_HEADER_TYPE_START //struct seq_file aka seq_file
        PS_HEADER_MEMBER(buf, buf)
        PS_HEADER_MEMBER(size, size)
        PS_HEADER_MEMBER(from, from)
        PS_HEADER_MEMBER(count, count)
        PS_HEADER_MEMBER(pad_until, pad_until)
        PS_HEADER_MEMBER(index, index)
        PS_HEADER_MEMBER(read_pos, read_pos)
        PS_HEADER_MEMBER(version, version)
        PS_HEADER_MEMBER(lock, lock)
        PS_HEADER_MEMBER(op, op)
        PS_HEADER_MEMBER(poll_event, poll_event)
        PS_HEADER_MEMBER(file, file)
        PS_HEADER_MEMBER(private, private)
    PS_HEADER_TYPE_END(struct seq_file, seq_file)
    PS_HEADER_TYPE_START //struct seq_operations aka seq_operations
        PS_HEADER_MEMBER(start, start)
        PS_HEADER_MEMBER(stop, stop)
        PS_HEADER_MEMBER(next, next)
        PS_HEADER_MEMBER(show, show)
    PS_HEADER_TYPE_END(struct seq_operations, seq_operations)
    PS_HEADER_TYPE_START //struct core_state aka core_state
        PS_HEADER_MEMBER(dumper, dumper)
        PS_HEADER_MEMBER(nr_threads, nr_threads)
        PS_HEADER_MEMBER(startup, startup)
    PS_HEADER_TYPE_END(struct core_state, core_state)
    PS_HEADER_TYPE_START //struct core_thread aka core_thread
        PS_HEADER_MEMBER(task, task)
        PS_HEADER_MEMBER(next, next)
    PS_HEADER_TYPE_END(struct core_thread, core_thread)
    PS_HEADER_TYPE_START //struct mm_struct aka mm_struct
        PS_HEADER_MEMBER(mmap, mmap)
        PS_HEADER_MEMBER(mm_rb, mm_rb)
        PS_HEADER_MEMBER(vmacache_seqnum, vmacache_seqnum)
        PS_HEADER_MEMBER(get_unmapped_area, get_unmapped_area)
        PS_HEADER_MEMBER(mmap_base, mmap_base)
        PS_HEADER_MEMBER(mmap_legacy_base, mmap_legacy_base)
        PS_HEADER_MEMBER(task_size, task_size)
        PS_HEADER_MEMBER(highest_vm_end, highest_vm_end)
        PS_HEADER_MEMBER(pgd, pgd)
        PS_HEADER_MEMBER(mm_users, mm_users)
        PS_HEADER_MEMBER(mm_count, mm_count)
        PS_HEADER_MEMBER(map_count, map_count)
        PS_HEADER_MEMBER(page_table_lock, page_table_lock)
        PS_HEADER_MEMBER(mmap_sem, mmap_sem)
        PS_HEADER_MEMBER(mmlist, mmlist)
        PS_HEADER_MEMBER(hiwater_rss, hiwater_rss)
        PS_HEADER_MEMBER(hiwater_vm, hiwater_vm)
        PS_HEADER_MEMBER(total_vm, total_vm)
        PS_HEADER_MEMBER(locked_vm, locked_vm)
        PS_HEADER_MEMBER(pinned_vm, pinned_vm)
        PS_HEADER_MEMBER(data_vm, data_vm)
        PS_HEADER_MEMBER(exec_vm, exec_vm)
        PS_HEADER_MEMBER(stack_vm, stack_vm)
        PS_HEADER_MEMBER(def_flags, def_flags)
        PS_HEADER_MEMBER(start_code, start_code)
        PS_HEADER_MEMBER(end_code, end_code)
        PS_HEADER_MEMBER(start_data, start_data)
        PS_HEADER_MEMBER(end_data, end_data)
        PS_HEADER_MEMBER(start_brk, start_brk)
        PS_HEADER_MEMBER(brk, brk)
        PS_HEADER_MEMBER(start_stack, start_stack)
        PS_HEADER_MEMBER(arg_start, arg_start)
        PS_HEADER_MEMBER(arg_end, arg_end)
        PS_HEADER_MEMBER(env_start, env_start)
        PS_HEADER_MEMBER(env_end, env_end)
        PS_HEADER_MEMBER(saved_auxv, saved_auxv)
        PS_HEADER_MEMBER(rss_stat, rss_stat)
        PS_HEADER_MEMBER(binfmt, binfmt)
        PS_HEADER_MEMBER(cpu_vm_mask_var, cpu_vm_mask_var)
        PS_HEADER_MEMBER(context, context)
        PS_HEADER_MEMBER(flags, flags)
        PS_HEADER_MEMBER(core_state, core_state)
        PS_HEADER_MEMBER(user_ns, user_ns)
        PS_HEADER_MEMBER(exe_file, exe_file)
        PS_HEADER_MEMBER(tlb_flush_pending, tlb_flush_pending)
        PS_HEADER_MEMBER(uprobes_state, uprobes_state)
        PS_HEADER_MEMBER(async_put_work, async_put_work)
    PS_HEADER_TYPE_END(struct mm_struct, mm_struct)
    PS_HEADER_TYPE_START //struct vm_area_struct aka vm_area_struct
        PS_HEADER_MEMBER(vm_start, vm_start)
        PS_HEADER_MEMBER(vm_end, vm_end)
        PS_HEADER_MEMBER(vm_next, vm_next)
        PS_HEADER_MEMBER(vm_prev, vm_prev)
        PS_HEADER_MEMBER(vm_rb, vm_rb)
        PS_HEADER_MEMBER(rb_subtree_gap, rb_subtree_gap)
        PS_HEADER_MEMBER(vm_mm, vm_mm)
        PS_HEADER_MEMBER(vm_page_prot, vm_page_prot)
        PS_HEADER_MEMBER(vm_flags, vm_flags)
        PS_HEADER_MEMBER(shared.rb, shared_rb)
        PS_HEADER_MEMBER(shared.rb_subtree_last, shared_rb_subtree_last)
        PS_HEADER_MEMBER(anon_vma_chain, anon_vma_chain)
        PS_HEADER_MEMBER(anon_vma, anon_vma)
        PS_HEADER_MEMBER(vm_ops, vm_ops)
        PS_HEADER_MEMBER(vm_pgoff, vm_pgoff)
        PS_HEADER_MEMBER(vm_file, vm_file)
        PS_HEADER_MEMBER(vm_private_data, vm_private_data)
        PS_HEADER_MEMBER(swap_readahead_info, swap_readahead_info)
        PS_HEADER_MEMBER(vm_userfaultfd_ctx, vm_userfaultfd_ctx)
    PS_HEADER_TYPE_END(struct vm_area_struct, vm_area_struct)
    PS_HEADER_TYPE_START //struct vm_region aka vm_region
        PS_HEADER_MEMBER(vm_rb, vm_rb)
        PS_HEADER_MEMBER(vm_flags, vm_flags)
        PS_HEADER_MEMBER(vm_start, vm_start)
        PS_HEADER_MEMBER(vm_end, vm_end)
        PS_HEADER_MEMBER(vm_top, vm_top)
        PS_HEADER_MEMBER(vm_pgoff, vm_pgoff)
        PS_HEADER_MEMBER(vm_file, vm_file)
        PS_HEADER_MEMBER(vm_usage, vm_usage)
    PS_HEADER_TYPE_END(struct vm_region, vm_region)
    PS_HEADER_TYPE_START //struct vm_fault aka vm_fault
        PS_HEADER_MEMBER(vma, vma)
        PS_HEADER_MEMBER(flags, flags)
        PS_HEADER_MEMBER(gfp_mask, gfp_mask)
        PS_HEADER_MEMBER(pgoff, pgoff)
        PS_HEADER_MEMBER(address, address)
        PS_HEADER_MEMBER(pmd, pmd)
        PS_HEADER_MEMBER(pud, pud)
        PS_HEADER_MEMBER(orig_pte, orig_pte)
        PS_HEADER_MEMBER(cow_page, cow_page)
        PS_HEADER_MEMBER(memcg, memcg)
        PS_HEADER_MEMBER(page, page)
        PS_HEADER_MEMBER(pte, pte)
        PS_HEADER_MEMBER(ptl, ptl)
        PS_HEADER_MEMBER(prealloc_pte, prealloc_pte)
    PS_HEADER_TYPE_END(struct vm_fault, vm_fault)
    PS_HEADER_TYPE_START //struct vm_operations_struct aka vm_operations_struct
        PS_HEADER_MEMBER(open, open)
        PS_HEADER_MEMBER(close, close)
        PS_HEADER_MEMBER(split, split)
        PS_HEADER_MEMBER(mremap, mremap)
        PS_HEADER_MEMBER(fault, fault)
        PS_HEADER_MEMBER(huge_fault, huge_fault)
        PS_HEADER_MEMBER(map_pages, map_pages)
        PS_HEADER_MEMBER(page_mkwrite, page_mkwrite)
        PS_HEADER_MEMBER(pfn_mkwrite, pfn_mkwrite)
        PS_HEADER_MEMBER(access, access)
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(find_special_page, find_special_page)
    PS_HEADER_TYPE_END(struct vm_operations_struct, vm_operations_struct)
    PS_HEADER_TYPE_START //struct vm_special_mapping aka vm_special_mapping
    PS_HEADER_TYPE_END(struct vm_special_mapping, vm_special_mapping)
    PS_HEADER_TYPE_START //struct page aka page
        PS_HEADER_MEMBER(flags, flags)
    PS_HEADER_TYPE_END(struct page, page)
    PS_HEADER_TYPE_START //struct scatterlist aka scatterlist
        PS_HEADER_MEMBER(page_link, page_link)
        PS_HEADER_MEMBER(offset, offset)
        PS_HEADER_MEMBER(length, length)
        PS_HEADER_MEMBER(dma_address, dma_address)
    PS_HEADER_TYPE_END(struct scatterlist, scatterlist)
    PS_HEADER_TYPE_START //struct usb_interface_descriptor aka usb_interface_descriptor
    PS_HEADER_TYPE_END(struct usb_interface_descriptor, usb_interface_descriptor)
    PS_HEADER_TYPE_START //struct usb_host_endpoint aka usb_host_endpoint
    PS_HEADER_TYPE_END(struct usb_host_endpoint, usb_host_endpoint)
    PS_HEADER_TYPE_START //struct usb_bus aka usb_bus
    PS_HEADER_TYPE_END(struct usb_bus, usb_bus)
    PS_HEADER_TYPE_START //struct usb_interface aka usb_interface
    PS_HEADER_TYPE_END(struct usb_interface, usb_interface)
    PS_HEADER_TYPE_START //struct usb_host_interface aka usb_host_interface
    PS_HEADER_TYPE_END(struct usb_host_interface, usb_host_interface)
    PS_HEADER_TYPE_START //struct usb_device aka usb_device
    PS_HEADER_TYPE_END(struct usb_device, usb_device)
    PS_HEADER_TYPE_START //struct usb_driver aka usb_driver
    PS_HEADER_TYPE_END(struct usb_driver, usb_driver)
    PS_HEADER_TYPE_START //struct usb_device_driver aka usb_device_driver
    PS_HEADER_TYPE_END(struct usb_device_driver, usb_device_driver)
    PS_HEADER_TYPE_START //struct usb_anchor aka usb_anchor
    PS_HEADER_TYPE_END(struct usb_anchor, usb_anchor)
    PS_HEADER_TYPE_START //struct usb_iso_packet_descriptor aka usb_iso_packet_descriptor
    PS_HEADER_TYPE_END(struct usb_iso_packet_descriptor, usb_iso_packet_descriptor)
    PS_HEADER_TYPE_START //struct usb_sg_request aka usb_sg_request
    PS_HEADER_TYPE_END(struct usb_sg_request, usb_sg_request)
    PS_HEADER_TYPE_START //struct usb_interface_cache aka usb_interface_cache
    PS_HEADER_TYPE_END(struct usb_interface_cache, usb_interface_cache)
    PS_HEADER_TYPE_START //struct usb_host_config aka usb_host_config
    PS_HEADER_TYPE_END(struct usb_host_config, usb_host_config)
    PS_HEADER_TYPE_START //struct usb_host_bos aka usb_host_bos
    PS_HEADER_TYPE_END(struct usb_host_bos, usb_host_bos)
    PS_HEADER_TYPE_START //struct urb aka urb
    PS_HEADER_TYPE_END(struct urb, urb)
    PS_HEADER_TYPE_START //struct usb_ss_ep_comp_descriptor aka usb_ss_ep_comp_descriptor
    PS_HEADER_TYPE_END(struct usb_ss_ep_comp_descriptor, usb_ss_ep_comp_descriptor)
    PS_HEADER_TYPE_START //struct usb_ssp_isoc_ep_comp_descriptor aka usb_ssp_isoc_ep_comp_descriptor
    PS_HEADER_TYPE_END(struct usb_ssp_isoc_ep_comp_descriptor, usb_ssp_isoc_ep_comp_descriptor)
    PS_HEADER_TYPE_START //struct usb_interface_assoc_descriptor aka usb_interface_assoc_descriptor
    PS_HEADER_TYPE_END(struct usb_interface_assoc_descriptor, usb_interface_assoc_descriptor)
    PS_HEADER_TYPE_START //struct usb_endpoint_descriptor aka usb_endpoint_descriptor
    PS_HEADER_TYPE_END(struct usb_endpoint_descriptor, usb_endpoint_descriptor)
    PS_HEADER_TYPE_START //struct usb_config_descriptor aka usb_config_descriptor
    PS_HEADER_TYPE_END(struct usb_config_descriptor, usb_config_descriptor)
    PS_HEADER_TYPE_START //struct notifier_block aka notifier_block
    PS_HEADER_TYPE_END(struct notifier_block, notifier_block)
    PS_HEADER_TYPE_START //struct atomic_notifier_head aka atomic_notifier_head
        PS_HEADER_MEMBER(head, head)
        PS_HEADER_MEMBER(lock, lock)
    PS_HEADER_TYPE_END(struct atomic_notifier_head, atomic_notifier_head)
    PS_HEADER_TYPE_START //struct blocking_notifier_head aka blocking_notifier_head
        PS_HEADER_MEMBER(rwsem, rwsem)
        PS_HEADER_MEMBER(head, head)
    PS_HEADER_TYPE_END(struct blocking_notifier_head, blocking_notifier_head)
    PS_HEADER_TYPE_START //struct raw_notifier_head aka raw_notifier_head
        PS_HEADER_MEMBER(head, head)
    PS_HEADER_TYPE_END(struct raw_notifier_head, raw_notifier_head)
    PS_HEADER_TYPE_START //struct srcu_notifier_head aka srcu_notifier_head
    PS_HEADER_TYPE_END(struct srcu_notifier_head, srcu_notifier_head)
    PS_HEADER_TYPE_START //struct pci_driver aka pci_driver
        PS_HEADER_MEMBER(node, node)
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(id_table, id_table)
        PS_HEADER_MEMBER(probe, probe)
        PS_HEADER_MEMBER(remove, remove)
        PS_HEADER_MEMBER(suspend, suspend)
        PS_HEADER_MEMBER(suspend_late, suspend_late)
        PS_HEADER_MEMBER(resume_early, resume_early)
        PS_HEADER_MEMBER(resume, resume)
        PS_HEADER_MEMBER(shutdown, shutdown)
        PS_HEADER_MEMBER(sriov_configure, sriov_configure)
        PS_HEADER_MEMBER(err_handler, err_handler)
        PS_HEADER_MEMBER(groups, groups)
        PS_HEADER_MEMBER(driver, driver)
        PS_HEADER_MEMBER(dynids, dynids)
    PS_HEADER_TYPE_END(struct pci_driver, pci_driver)
    PS_HEADER_TYPE_START //struct pci_device_id aka pci_device_id
        PS_HEADER_MEMBER(vendor, vendor)
        PS_HEADER_MEMBER(device, device)
        PS_HEADER_MEMBER(subvendor, subvendor)
        PS_HEADER_MEMBER(subdevice, subdevice)
        PS_HEADER_MEMBER(class, class)
        PS_HEADER_MEMBER(class_mask, class_mask)
        PS_HEADER_MEMBER(driver_data, driver_data)
    PS_HEADER_TYPE_END(struct pci_device_id, pci_device_id)
    PS_HEADER_TYPE_START //struct pci_bus aka pci_bus
        PS_HEADER_MEMBER(node, node)
        PS_HEADER_MEMBER(parent, parent)
        PS_HEADER_MEMBER(children, children)
        PS_HEADER_MEMBER(devices, devices)
        PS_HEADER_MEMBER(self, self)
        PS_HEADER_MEMBER(slots, slots)
        PS_HEADER_MEMBER(resource, resource)
        PS_HEADER_MEMBER(resources, resources)
        PS_HEADER_MEMBER(busn_res, busn_res)
        PS_HEADER_MEMBER(ops, ops)
        PS_HEADER_MEMBER(msi, msi)
        PS_HEADER_MEMBER(sysdata, sysdata)
        PS_HEADER_MEMBER(procdir, procdir)
        PS_HEADER_MEMBER(number, number)
        PS_HEADER_MEMBER(primary, primary)
        PS_HEADER_MEMBER(max_bus_speed, max_bus_speed)
        PS_HEADER_MEMBER(cur_bus_speed, cur_bus_speed)
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(bridge_ctl, bridge_ctl)
        PS_HEADER_MEMBER(bus_flags, bus_flags)
        PS_HEADER_MEMBER(bridge, bridge)
        PS_HEADER_MEMBER(dev, dev)
        PS_HEADER_MEMBER(legacy_io, legacy_io)
        PS_HEADER_MEMBER(legacy_mem, legacy_mem)
    PS_HEADER_TYPE_END(struct pci_bus, pci_bus)
    PS_HEADER_TYPE_START //struct pci_slot aka pci_slot
        PS_HEADER_MEMBER(bus, bus)
        PS_HEADER_MEMBER(list, list)
        PS_HEADER_MEMBER(hotplug, hotplug)
        PS_HEADER_MEMBER(number, number)
        PS_HEADER_MEMBER(kobj, kobj)
    PS_HEADER_TYPE_END(struct pci_slot, pci_slot)
    PS_HEADER_TYPE_START //struct pci_dev aka pci_dev
        PS_HEADER_MEMBER(bus_list, bus_list)
        PS_HEADER_MEMBER(bus, bus)
        PS_HEADER_MEMBER(subordinate, subordinate)
        PS_HEADER_MEMBER(sysdata, sysdata)
        PS_HEADER_MEMBER(procent, procent)
        PS_HEADER_MEMBER(slot, slot)
        PS_HEADER_MEMBER(devfn, devfn)
        PS_HEADER_MEMBER(vendor, vendor)
        PS_HEADER_MEMBER(device, device)
        PS_HEADER_MEMBER(subsystem_vendor, subsystem_vendor)
        PS_HEADER_MEMBER(subsystem_device, subsystem_device)
        PS_HEADER_MEMBER(class, class)
        PS_HEADER_MEMBER(revision, revision)
        PS_HEADER_MEMBER(hdr_type, hdr_type)
        PS_HEADER_MEMBER(aer_cap, aer_cap)
        PS_HEADER_MEMBER(pcie_cap, pcie_cap)
        PS_HEADER_MEMBER(msi_cap, msi_cap)
        PS_HEADER_MEMBER(msix_cap, msix_cap)
        PS_HEADER_MEMBER(rom_base_reg, rom_base_reg)
        PS_HEADER_MEMBER(pin, pin)
        PS_HEADER_MEMBER(pcie_flags_reg, pcie_flags_reg)
        PS_HEADER_MEMBER(dma_alias_mask, dma_alias_mask)
        PS_HEADER_MEMBER(driver, driver)
        PS_HEADER_MEMBER(dma_mask, dma_mask)
        PS_HEADER_MEMBER(dma_parms, dma_parms)
        PS_HEADER_MEMBER(current_state, current_state)
        PS_HEADER_MEMBER(pm_cap, pm_cap)
        PS_HEADER_MEMBER(d3_delay, d3_delay)
        PS_HEADER_MEMBER(d3cold_delay, d3cold_delay)
        PS_HEADER_MEMBER(link_state, link_state)
        PS_HEADER_MEMBER(error_state, error_state)
        PS_HEADER_MEMBER(dev, dev)
        PS_HEADER_MEMBER(cfg_size, cfg_size)
        PS_HEADER_MEMBER(irq, irq)
        PS_HEADER_MEMBER(resource, resource)
        PS_HEADER_MEMBER(match_driver, match_driver)
        PS_HEADER_MEMBER(dev_flags, dev_flags)
        PS_HEADER_MEMBER(enable_cnt, enable_cnt)
        PS_HEADER_MEMBER(saved_config_space, saved_config_space)
        PS_HEADER_MEMBER(saved_cap_space, saved_cap_space)
        PS_HEADER_MEMBER(rom_attr, rom_attr)
        PS_HEADER_MEMBER(rom_attr_enabled, rom_attr_enabled)
        PS_HEADER_MEMBER(res_attr, res_attr)
        PS_HEADER_MEMBER(res_attr_wc, res_attr_wc)
        PS_HEADER_MEMBER(msi_irq_groups, msi_irq_groups)
        PS_HEADER_MEMBER(vpd, vpd)
        PS_HEADER_MEMBER(ats_cap, ats_cap)
        PS_HEADER_MEMBER(ats_stu, ats_stu)
        PS_HEADER_MEMBER(ats_ref_cnt, ats_ref_cnt)
        PS_HEADER_MEMBER(pri_reqs_alloc, pri_reqs_alloc)
        PS_HEADER_MEMBER(pasid_features, pasid_features)
        PS_HEADER_MEMBER(rom, rom)
        PS_HEADER_MEMBER(romlen, romlen)
        PS_HEADER_MEMBER(driver_override, driver_override)
        PS_HEADER_MEMBER(priv_flags, priv_flags)
    PS_HEADER_TYPE_END(struct pci_dev, pci_dev)
    PS_HEADER_TYPE_START //struct pci_error_handlers aka pci_error_handlers
        PS_HEADER_MEMBER(error_detected, error_detected)
        PS_HEADER_MEMBER(mmio_enabled, mmio_enabled)
        PS_HEADER_MEMBER(slot_reset, slot_reset)
        PS_HEADER_MEMBER(reset_prepare, reset_prepare)
        PS_HEADER_MEMBER(reset_done, reset_done)
        PS_HEADER_MEMBER(resume, resume)
    PS_HEADER_TYPE_END(struct pci_error_handlers, pci_error_handlers)
    PS_HEADER_TYPE_START //struct pci_dynids aka pci_dynids
        PS_HEADER_MEMBER(lock, lock)
        PS_HEADER_MEMBER(list, list)
    PS_HEADER_TYPE_END(struct pci_dynids, pci_dynids)
    PS_HEADER_TYPE_START //struct pci_bus_region aka pci_bus_region
        PS_HEADER_MEMBER(start, start)
        PS_HEADER_MEMBER(end, end)
    PS_HEADER_TYPE_END(struct pci_bus_region, pci_bus_region)
    PS_HEADER_TYPE_START //struct pci_host_bridge aka pci_host_bridge
        PS_HEADER_MEMBER(dev, dev)
        PS_HEADER_MEMBER(bus, bus)
        PS_HEADER_MEMBER(ops, ops)
        PS_HEADER_MEMBER(sysdata, sysdata)
        PS_HEADER_MEMBER(busnr, busnr)
        PS_HEADER_MEMBER(windows, windows)
        PS_HEADER_MEMBER(swizzle_irq, swizzle_irq)
        PS_HEADER_MEMBER(map_irq, map_irq)
        PS_HEADER_MEMBER(release_fn, release_fn)
        PS_HEADER_MEMBER(release_data, release_data)
        PS_HEADER_MEMBER(msi, msi)
        PS_HEADER_MEMBER(align_resource, align_resource)
        PS_HEADER_MEMBER(private, private)
    PS_HEADER_TYPE_END(struct pci_host_bridge, pci_host_bridge)
    PS_HEADER_TYPE_START //struct pci_ops aka pci_ops
        PS_HEADER_MEMBER(add_bus, add_bus)
        PS_HEADER_MEMBER(remove_bus, remove_bus)
        PS_HEADER_MEMBER(map_bus, map_bus)
        PS_HEADER_MEMBER(read, read)
        PS_HEADER_MEMBER(write, write)
    PS_HEADER_TYPE_END(struct pci_ops, pci_ops)
    PS_HEADER_TYPE_START //struct pci_cap_saved_state aka pci_cap_saved_state
    PS_HEADER_TYPE_END(struct pci_cap_saved_state, pci_cap_saved_state)
    PS_HEADER_TYPE_START //struct device aka device
        PS_HEADER_MEMBER(parent, parent)
        PS_HEADER_MEMBER(p, p)
        PS_HEADER_MEMBER(kobj, kobj)
        PS_HEADER_MEMBER(init_name, init_name)
        PS_HEADER_MEMBER(type, type)
        PS_HEADER_MEMBER(mutex, mutex)
        PS_HEADER_MEMBER(bus, bus)
        PS_HEADER_MEMBER(driver, driver)
        PS_HEADER_MEMBER(platform_data, platform_data)
        PS_HEADER_MEMBER(driver_data, driver_data)
        PS_HEADER_MEMBER(links, links)
        PS_HEADER_MEMBER(power, power)
        PS_HEADER_MEMBER(pm_domain, pm_domain)
        PS_HEADER_MEMBER(msi_domain, msi_domain)
        PS_HEADER_MEMBER(msi_list, msi_list)
        PS_HEADER_MEMBER(numa_node, numa_node)
        PS_HEADER_MEMBER(dma_ops, dma_ops)
        PS_HEADER_MEMBER(dma_mask, dma_mask)
        PS_HEADER_MEMBER(coherent_dma_mask, coherent_dma_mask)
        PS_HEADER_MEMBER(dma_pfn_offset, dma_pfn_offset)
        PS_HEADER_MEMBER(dma_parms, dma_parms)
        PS_HEADER_MEMBER(dma_pools, dma_pools)
        PS_HEADER_MEMBER(dma_mem, dma_mem)
        PS_HEADER_MEMBER(archdata, archdata)
        PS_HEADER_MEMBER(of_node, of_node)
        PS_HEADER_MEMBER(fwnode, fwnode)
        PS_HEADER_MEMBER(devt, devt)
        PS_HEADER_MEMBER(id, id)
        PS_HEADER_MEMBER(devres_lock, devres_lock)
        PS_HEADER_MEMBER(devres_head, devres_head)
        PS_HEADER_MEMBER(knode_class, knode_class)
        PS_HEADER_MEMBER(class, class)
        PS_HEADER_MEMBER(groups, groups)
        PS_HEADER_MEMBER(release, release)
        PS_HEADER_MEMBER(iommu_group, iommu_group)
        PS_HEADER_MEMBER(iommu_fwspec, iommu_fwspec)
    PS_HEADER_TYPE_END(struct device, device)
    PS_HEADER_TYPE_START //struct device_type aka device_type
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(groups, groups)
        PS_HEADER_MEMBER(uevent, uevent)
        PS_HEADER_MEMBER(devnode, devnode)
        PS_HEADER_MEMBER(release, release)
        PS_HEADER_MEMBER(pm, pm)
    PS_HEADER_TYPE_END(struct device_type, device_type)
    PS_HEADER_TYPE_START //struct device_node aka device_node
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(type, type)
        PS_HEADER_MEMBER(phandle, phandle)
        PS_HEADER_MEMBER(full_name, full_name)
        PS_HEADER_MEMBER(fwnode, fwnode)
        PS_HEADER_MEMBER(properties, properties)
        PS_HEADER_MEMBER(deadprops, deadprops)
        PS_HEADER_MEMBER(parent, parent)
        PS_HEADER_MEMBER(child, child)
        PS_HEADER_MEMBER(sibling, sibling)
        PS_HEADER_MEMBER(_flags, _flags)
        PS_HEADER_MEMBER(data, data)
    PS_HEADER_TYPE_END(struct device_node, device_node)
    PS_HEADER_TYPE_START //struct device_attribute aka device_attribute
        PS_HEADER_MEMBER(attr, attr)
        PS_HEADER_MEMBER(store, store)
        PS_HEADER_MEMBER(show, show)
    PS_HEADER_TYPE_END(struct device_attribute, device_attribute)
    PS_HEADER_TYPE_START //struct device_driver aka device_driver
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(bus, bus)
        PS_HEADER_MEMBER(owner, owner)
        PS_HEADER_MEMBER(mod_name, mod_name)
        PS_HEADER_MEMBER(suppress_bind_attrs, suppress_bind_attrs)
        PS_HEADER_MEMBER(probe_type, probe_type)
        PS_HEADER_MEMBER(of_match_table, of_match_table)
        PS_HEADER_MEMBER(acpi_match_table, acpi_match_table)
        PS_HEADER_MEMBER(probe, probe)
        PS_HEADER_MEMBER(remove, remove)
        PS_HEADER_MEMBER(shutdown, shutdown)
        PS_HEADER_MEMBER(suspend, suspend)
        PS_HEADER_MEMBER(resume, resume)
        PS_HEADER_MEMBER(groups, groups)
        PS_HEADER_MEMBER(pm, pm)
        PS_HEADER_MEMBER(p, p)
    PS_HEADER_TYPE_END(struct device_driver, device_driver)
    PS_HEADER_TYPE_START //struct device_link aka device_link
        PS_HEADER_MEMBER(supplier, supplier)
        PS_HEADER_MEMBER(s_node, s_node)
        PS_HEADER_MEMBER(consumer, consumer)
        PS_HEADER_MEMBER(c_node, c_node)
        PS_HEADER_MEMBER(status, status)
        PS_HEADER_MEMBER(flags, flags)
        PS_HEADER_MEMBER(rpm_active, rpm_active)
    PS_HEADER_TYPE_END(struct device_link, device_link)
    PS_HEADER_TYPE_START //struct dev_links_info aka dev_links_info
        PS_HEADER_MEMBER(suppliers, suppliers)
        PS_HEADER_MEMBER(consumers, consumers)
        PS_HEADER_MEMBER(status, status)
    PS_HEADER_TYPE_END(struct dev_links_info, dev_links_info)
    PS_HEADER_TYPE_START //struct dev_ext_attribute aka dev_ext_attribute
        PS_HEADER_MEMBER(attr, attr)
        PS_HEADER_MEMBER(var, var)
    PS_HEADER_TYPE_END(struct dev_ext_attribute, dev_ext_attribute)
    PS_HEADER_TYPE_START //struct class aka class
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(owner, owner)
        PS_HEADER_MEMBER(class_groups, class_groups)
        PS_HEADER_MEMBER(dev_groups, dev_groups)
        PS_HEADER_MEMBER(dev_kobj, dev_kobj)
        PS_HEADER_MEMBER(dev_uevent, dev_uevent)
        PS_HEADER_MEMBER(devnode, devnode)
        PS_HEADER_MEMBER(class_release, class_release)
        PS_HEADER_MEMBER(dev_release, dev_release)
        PS_HEADER_MEMBER(shutdown_pre, shutdown_pre)
        PS_HEADER_MEMBER(ns_type, ns_type)
        PS_HEADER_MEMBER(namespace, namespace)
        PS_HEADER_MEMBER(pm, pm)
        PS_HEADER_MEMBER(p, p)
    PS_HEADER_TYPE_END(struct class, class)
    PS_HEADER_TYPE_START //struct class_dev_iter aka class_dev_iter
        PS_HEADER_MEMBER(ki, ki)
        PS_HEADER_MEMBER(type, type)
    PS_HEADER_TYPE_END(struct class_dev_iter, class_dev_iter)
    PS_HEADER_TYPE_START //struct class_interface aka class_interface
        PS_HEADER_MEMBER(node, node)
        PS_HEADER_MEMBER(class, class)
        PS_HEADER_MEMBER(add_dev, add_dev)
        PS_HEADER_MEMBER(remove_dev, remove_dev)
    PS_HEADER_TYPE_END(struct class_interface, class_interface)
    PS_HEADER_TYPE_START //struct class_attribute_string aka class_attribute_string
        PS_HEADER_MEMBER(attr, attr)
        PS_HEADER_MEMBER(str, str)
    PS_HEADER_TYPE_END(struct class_attribute_string, class_attribute_string)
    PS_HEADER_TYPE_START //struct class_attribute aka class_attribute
        PS_HEADER_MEMBER(attr, attr)
        PS_HEADER_MEMBER(show, show)
        PS_HEADER_MEMBER(store, store)
    PS_HEADER_TYPE_END(struct class_attribute, class_attribute)
    PS_HEADER_TYPE_START //struct bus_type aka bus_type
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(dev_name, dev_name)
        PS_HEADER_MEMBER(dev_root, dev_root)
        PS_HEADER_MEMBER(bus_groups, bus_groups)
        PS_HEADER_MEMBER(dev_groups, dev_groups)
        PS_HEADER_MEMBER(drv_groups, drv_groups)
        PS_HEADER_MEMBER(match, match)
        PS_HEADER_MEMBER(uevent, uevent)
        PS_HEADER_MEMBER(probe, probe)
        PS_HEADER_MEMBER(remove, remove)
        PS_HEADER_MEMBER(shutdown, shutdown)
        PS_HEADER_MEMBER(online, online)
        PS_HEADER_MEMBER(offline, offline)
        PS_HEADER_MEMBER(suspend, suspend)
        PS_HEADER_MEMBER(resume, resume)
        PS_HEADER_MEMBER(num_vf, num_vf)
        PS_HEADER_MEMBER(pm, pm)
        PS_HEADER_MEMBER(iommu_ops, iommu_ops)
        PS_HEADER_MEMBER(p, p)
        PS_HEADER_MEMBER(lock_key, lock_key)
    PS_HEADER_TYPE_END(struct bus_type, bus_type)
    PS_HEADER_TYPE_START //struct bus_attribute aka bus_attribute
        PS_HEADER_MEMBER(attr, attr)
        PS_HEADER_MEMBER(show, show)
        PS_HEADER_MEMBER(store, store)
    PS_HEADER_TYPE_END(struct bus_attribute, bus_attribute)
    PS_HEADER_TYPE_START //struct irq_domain aka irq_domain
    PS_HEADER_TYPE_END(struct irq_domain, irq_domain)
    PS_HEADER_TYPE_START //struct attribute_group aka attribute_group
        PS_HEADER_MEMBER(name, name)
        PS_HEADER_MEMBER(is_visible, is_visible)
        PS_HEADER_MEMBER(is_bin_visible, is_bin_visible)
        PS_HEADER_MEMBER(attrs, attrs)
        PS_HEADER_MEMBER(bin_attrs, bin_attrs)
    PS_HEADER_TYPE_END(struct attribute_group, attribute_group)
    PS_HEADER_TYPE_START //struct bin_attribute aka bin_attribute
        PS_HEADER_MEMBER(attr, attr)
        PS_HEADER_MEMBER(size, size)
        PS_HEADER_MEMBER(private, private)
        PS_HEADER_MEMBER(read, read)
        PS_HEADER_MEMBER(mmap, mmap)
    PS_HEADER_TYPE_END(struct bin_attribute, bin_attribute)
    PS_HEADER_TYPE_START //struct sysfs_ops aka sysfs_ops
        PS_HEADER_MEMBER(show, show)
        PS_HEADER_MEMBER(store, store)
    PS_HEADER_TYPE_END(struct sysfs_ops, sysfs_ops)
    PS_HEADER_TYPE_START //struct cdev aka cdev
        PS_HEADER_MEMBER(kobj, kobj)
        PS_HEADER_MEMBER(owner, opsj)
        PS_HEADER_MEMBER(list, list)
        PS_HEADER_MEMBER(dev, dev)
        PS_HEADER_MEMBER(count, count)
    PS_HEADER_TYPE_END(struct cdev, cdev)
    PS_HEADER_TYPE_START //struct dev_pm_ops aka dev_pm_ops
        PS_HEADER_MEMBER(prepare, prepare)
        PS_HEADER_MEMBER(complete, complete)
        PS_HEADER_MEMBER(suspend, suspend)
        PS_HEADER_MEMBER(resume, resume)
        PS_HEADER_MEMBER(freeze, freeze)
        PS_HEADER_MEMBER(thaw, thaw)
        PS_HEADER_MEMBER(poweroff, poweroff)
        PS_HEADER_MEMBER(restore, restore)
        PS_HEADER_MEMBER(suspend_late, suspend_late)
        PS_HEADER_MEMBER(resume_early, resume_early)
        PS_HEADER_MEMBER(freeze_late, freeze_late)
        PS_HEADER_MEMBER(thaw_early, thaw_early)
        PS_HEADER_MEMBER(poweroff_late, poweroff_late)
        PS_HEADER_MEMBER(restore_early, restore_early)
        PS_HEADER_MEMBER(suspend_noirq, suspend_noirq)
        PS_HEADER_MEMBER(resume_noirq, resume_noirq)
        PS_HEADER_MEMBER(freeze_noirq, freeze_noirq)
        PS_HEADER_MEMBER(thaw_noirq, thaw_noirq)
        PS_HEADER_MEMBER(poweroff_noirq, poweroff_noirq)
        PS_HEADER_MEMBER(restore_noirq, restore_noirq)
        PS_HEADER_MEMBER(runtime_suspend, runtime_suspend)
        PS_HEADER_MEMBER(runtime_resume, runtime_resume)
        PS_HEADER_MEMBER(runtime_idle, runtime_idle)
    PS_HEADER_TYPE_END(struct dev_pm_ops, dev_pm_ops)
    PS_HEADER_TYPE_START //struct dev_pm_domain aka dev_pm_domain
        PS_HEADER_MEMBER(ops, ops)
        PS_HEADER_MEMBER(detach, detach)
        PS_HEADER_MEMBER(activate, activate)
        PS_HEADER_MEMBER(sync, sync)
        PS_HEADER_MEMBER(dismiss, dismiss)
    PS_HEADER_TYPE_END(struct dev_pm_domain, dev_pm_domain)
    PS_HEADER_TYPE_START //struct pm_subsys_data aka pm_subsys_data
        PS_HEADER_MEMBER(lock, lock)
        PS_HEADER_MEMBER(refcount, refcount)
    PS_HEADER_TYPE_END(struct pm_subsys_data, pm_subsys_data)
    PS_HEADER_TYPE_START //struct module aka module
    PS_HEADER_TYPE_END(struct module, module)
    PS_HEADER_TYPE_START //struct module_attribute aka module_attribute
    PS_HEADER_TYPE_END(struct module_attribute, module_attribute)
    PS_HEADER_TYPE_START //struct module_kobject aka module_kobject
    PS_HEADER_TYPE_END(struct module_kobject, module_kobject)
    PS_HEADER_TYPE_START //struct module_version_attribute aka module_version_attribute
    PS_HEADER_TYPE_END(struct module_version_attribute, module_version_attribute)
    PS_HEADER_TYPE_START //struct module_use aka module_use
    PS_HEADER_TYPE_END(struct module_use, module_use)
    PS_HEADER_TYPE_START //struct module_layout aka module_layout
    PS_HEADER_TYPE_END(struct module_layout, module_layout)
    PS_HEADER_TYPE_START //struct mod_tree_node aka mod_tree_node
    PS_HEADER_TYPE_END(struct mod_tree_node, mod_tree_node)
    PS_HEADER_TYPE_START //struct mod_kallsyms aka mod_kallsyms
    PS_HEADER_TYPE_END(struct mod_kallsyms, mod_kallsyms)
    PS_HEADER_TYPE_START //struct dma_map_ops aka dma_map_ops
        PS_HEADER_MEMBER(alloc, alloc)
        PS_HEADER_MEMBER(free, free)
        PS_HEADER_MEMBER(mmap, mmap)
        PS_HEADER_MEMBER(get_sgtable, get_sgtable)
        PS_HEADER_MEMBER(map_page, map_page)
        PS_HEADER_MEMBER(unmap_page, unmap_page)
        PS_HEADER_MEMBER(map_sg, map_sg)
        PS_HEADER_MEMBER(unmap_sg, unmap_sg)
        PS_HEADER_MEMBER(map_resource, map_resource)
        PS_HEADER_MEMBER(unmap_resource, unmap_resource)
        PS_HEADER_MEMBER(sync_single_for_cpu, sync_single_for_cpu)
        PS_HEADER_MEMBER(sync_single_for_device, sync_single_for_device)
        PS_HEADER_MEMBER(sync_sg_for_cpu, sync_sg_for_cpu)
        PS_HEADER_MEMBER(sync_sg_for_device, sync_sg_for_device)
        PS_HEADER_MEMBER(mapping_error, mapping_error)
        PS_HEADER_MEMBER(dma_supported, dma_supported)
        PS_HEADER_MEMBER(is_phys, is_phys)
    PS_HEADER_TYPE_END(struct dma_map_ops, dma_map_ops)
    PS_HEADER_TYPE_START //struct qstr aka qstr
        PS_HEADER_MEMBER(hash_len, hash_len)
        PS_HEADER_MEMBER(name, name)
    PS_HEADER_TYPE_END(struct qstr, qstr)
    PS_HEADER_TYPE_START //struct dentry aka dentry
        PS_HEADER_MEMBER(d_flags, d_flags)
        PS_HEADER_MEMBER(d_seq, d_seq)
        PS_HEADER_MEMBER(d_hash, d_hash)
        PS_HEADER_MEMBER(d_parent, d_parent)
        PS_HEADER_MEMBER(d_name, d_name)
        PS_HEADER_MEMBER(d_inode, d_inode)
        PS_HEADER_MEMBER(d_iname, d_iname)
        PS_HEADER_MEMBER(d_lockref, d_lockref)
        PS_HEADER_MEMBER(d_op, d_op)
        PS_HEADER_MEMBER(d_sb, d_sb)
        PS_HEADER_MEMBER(d_time, d_time)
        PS_HEADER_MEMBER(d_fsdata, d_fsdata)
        PS_HEADER_MEMBER(d_lru, d_lru)
        PS_HEADER_MEMBER(d_wait, d_wait)
        PS_HEADER_MEMBER(d_child, d_child)
        PS_HEADER_MEMBER(d_subdirs, d_subdirs)
        PS_HEADER_MEMBER(d_u.d_alias, d_u_d_alias)
        PS_HEADER_MEMBER(d_u.d_alias, d_u_generic)
    PS_HEADER_TYPE_END(struct dentry, dentry)
    PS_HEADER_TYPE_START //struct dentry_operations aka dentry_operations
        PS_HEADER_MEMBER(d_revalidate, d_revalidate)
        PS_HEADER_MEMBER(d_weak_revalidate, d_weak_revalidate)
        PS_HEADER_MEMBER(d_hash, d_hash)
        PS_HEADER_MEMBER(d_compare, d_compare)
        PS_HEADER_MEMBER(d_delete, d_delete)
        PS_HEADER_MEMBER(d_init, d_init)
        PS_HEADER_MEMBER(d_release, d_release)
        PS_HEADER_MEMBER(d_prune, d_prune)
        PS_HEADER_MEMBER(d_iput, d_iput)
        PS_HEADER_MEMBER(d_dname, d_dname)
        PS_HEADER_MEMBER(d_automount, d_automount)
        PS_HEADER_MEMBER(d_manage, d_manage)
        PS_HEADER_MEMBER(d_real, d_real)
    PS_HEADER_TYPE_END(struct dentry_operations, dentry_operations)
    PS_HEADER_TYPE_START //struct dentry_stat_t aka dentry_stat
        PS_HEADER_MEMBER(nr_dentry, nr_dentry)
        PS_HEADER_MEMBER(nr_unused, nr_unused)
        PS_HEADER_MEMBER(age_limit, age_limit)
        PS_HEADER_MEMBER(want_pages, want_pages)
        PS_HEADER_MEMBER(dummy, dummy)
    PS_HEADER_TYPE_END(struct dentry_stat_t, dentry_stat)
    PS_HEADER_TYPE_START //struct wait_queue_entry aka wait_queue_entry
        PS_HEADER_MEMBER(flags, flags)
        PS_HEADER_MEMBER(private, private)
        PS_HEADER_MEMBER(entry, entry)
        PS_HEADER_MEMBER(func, func)
    PS_HEADER_TYPE_END(struct wait_queue_entry, wait_queue_entry)
PS_HEADER_GLOBAL_END

#ifndef BOOTSTRAP
PS_HEADER_INIT_TYPE(refcount_struct)
PS_HEADER_INIT_MEMBER(refcount_struct, refs)
PS_HEADER_INIT_TYPE(kref)
PS_HEADER_INIT_MEMBER(kref, refcount)
PS_HEADER_INIT_TYPE(kobject)
PS_HEADER_INIT_MEMBER(kobject, name)
PS_HEADER_INIT_MEMBER(kobject, entry)
PS_HEADER_INIT_MEMBER(kobject, parent)
PS_HEADER_INIT_MEMBER(kobject, kset)
PS_HEADER_INIT_MEMBER(kobject, ktype)
PS_HEADER_INIT_MEMBER(kobject, sd)
PS_HEADER_INIT_MEMBER(kobject, kref)
PS_HEADER_INIT_TYPE(resource)
PS_HEADER_INIT_MEMBER(resource, start)
PS_HEADER_INIT_MEMBER(resource, end)
PS_HEADER_INIT_MEMBER(resource, name)
PS_HEADER_INIT_MEMBER(resource, flags)
PS_HEADER_INIT_MEMBER(resource, desc)
PS_HEADER_INIT_MEMBER(resource, siblings)
PS_HEADER_INIT_MEMBER(resource, child)
PS_HEADER_INIT_TYPE(vtime)
PS_HEADER_INIT_MEMBER(vtime, seqcount)
PS_HEADER_INIT_MEMBER(vtime, starttime)
PS_HEADER_INIT_MEMBER(vtime, state)
PS_HEADER_INIT_MEMBER(vtime, utime)
PS_HEADER_INIT_MEMBER(vtime, stime)
PS_HEADER_INIT_MEMBER(vtime, gtime)
PS_HEADER_INIT_TYPE(prev_cputime)
PS_HEADER_INIT_TYPE(load_weight)
PS_HEADER_INIT_MEMBER(load_weight, weight)
PS_HEADER_INIT_MEMBER(load_weight, inv_weight)
PS_HEADER_INIT_TYPE(sched_avg)
PS_HEADER_INIT_MEMBER(sched_avg, last_update_time)
PS_HEADER_INIT_MEMBER(sched_avg, load_sum)
PS_HEADER_INIT_MEMBER(sched_avg, util_sum)
PS_HEADER_INIT_MEMBER(sched_avg, period_contrib)
PS_HEADER_INIT_MEMBER(sched_avg, load_avg)
PS_HEADER_INIT_MEMBER(sched_avg, util_avg)
PS_HEADER_INIT_TYPE(sched_statistics)
PS_HEADER_INIT_MEMBER(sched_statistics, wait_start)
PS_HEADER_INIT_MEMBER(sched_statistics, wait_max)
PS_HEADER_INIT_MEMBER(sched_statistics, wait_count)
PS_HEADER_INIT_MEMBER(sched_statistics, wait_sum)
PS_HEADER_INIT_MEMBER(sched_statistics, iowait_count)
PS_HEADER_INIT_MEMBER(sched_statistics, iowait_sum)
PS_HEADER_INIT_MEMBER(sched_statistics, sleep_start)
PS_HEADER_INIT_MEMBER(sched_statistics, sleep_max)
PS_HEADER_INIT_MEMBER(sched_statistics, sum_sleep_runtime)
PS_HEADER_INIT_MEMBER(sched_statistics, block_start)
PS_HEADER_INIT_MEMBER(sched_statistics, block_max)
PS_HEADER_INIT_MEMBER(sched_statistics, exec_max)
PS_HEADER_INIT_MEMBER(sched_statistics, slice_max)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_migrations_cold)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_failed_migrations_affine)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_failed_migrations_running)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_failed_migrations_hot)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_forced_migrations)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_wakeups)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_wakeups_sync)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_wakeups_migrate)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_wakeups_local)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_wakeups_remote)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_wakeups_affine)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_wakeups_affine_attempts)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_wakeups_passive)
PS_HEADER_INIT_MEMBER(sched_statistics, nr_wakeups_idle)
PS_HEADER_INIT_TYPE(sched_entity)
PS_HEADER_INIT_MEMBER(sched_entity, load)
PS_HEADER_INIT_MEMBER(sched_entity, run_node)
PS_HEADER_INIT_MEMBER(sched_entity, group_node)
PS_HEADER_INIT_MEMBER(sched_entity, on_rq)
PS_HEADER_INIT_MEMBER(sched_entity, exec_start)
PS_HEADER_INIT_MEMBER(sched_entity, sum_exec_runtime)
PS_HEADER_INIT_MEMBER(sched_entity, vruntime)
PS_HEADER_INIT_MEMBER(sched_entity, prev_sum_exec_runtime)
PS_HEADER_INIT_MEMBER(sched_entity, nr_migrations)
PS_HEADER_INIT_MEMBER(sched_entity, statistics)
PS_HEADER_INIT_MEMBER(sched_entity, depth)
PS_HEADER_INIT_MEMBER(sched_entity, parent)
PS_HEADER_INIT_MEMBER(sched_entity, cfs_rq)
PS_HEADER_INIT_MEMBER(sched_entity, my_q)
PS_HEADER_INIT_MEMBER(sched_entity, avg)
PS_HEADER_INIT_TYPE(sched_rt_entity)
PS_HEADER_INIT_MEMBER(sched_rt_entity, run_list)
PS_HEADER_INIT_MEMBER(sched_rt_entity, timeout)
PS_HEADER_INIT_MEMBER(sched_rt_entity, watchdog_stamp)
PS_HEADER_INIT_MEMBER(sched_rt_entity, time_slice)
PS_HEADER_INIT_MEMBER(sched_rt_entity, on_rq)
PS_HEADER_INIT_MEMBER(sched_rt_entity, on_list)
PS_HEADER_INIT_MEMBER(sched_rt_entity, back)
PS_HEADER_INIT_MEMBER(sched_rt_entity, parent)
PS_HEADER_INIT_MEMBER(sched_rt_entity, rt_rq)
PS_HEADER_INIT_MEMBER(sched_rt_entity, my_q)
PS_HEADER_INIT_TYPE(sched_dl_entity)
PS_HEADER_INIT_MEMBER(sched_dl_entity, rb_node)
PS_HEADER_INIT_MEMBER(sched_dl_entity, dl_runtime)
PS_HEADER_INIT_MEMBER(sched_dl_entity, dl_deadline)
PS_HEADER_INIT_MEMBER(sched_dl_entity, dl_period)
PS_HEADER_INIT_MEMBER(sched_dl_entity, dl_bw)
PS_HEADER_INIT_MEMBER(sched_dl_entity, dl_density)
PS_HEADER_INIT_MEMBER(sched_dl_entity, runtime)
PS_HEADER_INIT_MEMBER(sched_dl_entity, deadline)
PS_HEADER_INIT_MEMBER(sched_dl_entity, flags)
PS_HEADER_INIT_MEMBER(sched_dl_entity, dl_throttled)
PS_HEADER_INIT_MEMBER(sched_dl_entity, dl_boosted)
PS_HEADER_INIT_MEMBER(sched_dl_entity, dl_yielded)
PS_HEADER_INIT_MEMBER(sched_dl_entity, dl_non_contending)
PS_HEADER_INIT_MEMBER(sched_dl_entity, dl_timer)
PS_HEADER_INIT_MEMBER(sched_dl_entity, inactive_timer)
PS_HEADER_INIT_TYPE(sd_data)
PS_HEADER_INIT_TYPE(sched_domain)
PS_HEADER_INIT_TYPE(sched_domain_topology_level)
PS_HEADER_INIT_TYPE(sched_domain_shared)
PS_HEADER_INIT_TYPE(task_delay_info)
PS_HEADER_INIT_TYPE(wake_q_node)
PS_HEADER_INIT_TYPE(sighand_struct)
PS_HEADER_INIT_TYPE(pacct_struct)
PS_HEADER_INIT_TYPE(cpu_itimer)
PS_HEADER_INIT_TYPE(task_cputime_atomic)
PS_HEADER_INIT_TYPE(thread_group_cputimer)
PS_HEADER_INIT_TYPE(signal_struct)
PS_HEADER_INIT_TYPE(rw_semaphore)
PS_HEADER_INIT_TYPE(spinlock_t)
PS_HEADER_INIT_TYPE(group_info)
PS_HEADER_INIT_TYPE(cred)
PS_HEADER_INIT_TYPE(user_struct)
PS_HEADER_INIT_MEMBER(user_struct, __count)
PS_HEADER_INIT_MEMBER(user_struct, processes)
PS_HEADER_INIT_MEMBER(user_struct, sigpending)
PS_HEADER_INIT_MEMBER(user_struct, locked_shm)
PS_HEADER_INIT_MEMBER(user_struct, unix_inflight)
PS_HEADER_INIT_MEMBER(user_struct, pipe_bufs)
PS_HEADER_INIT_MEMBER(user_struct, uid_keyring)
PS_HEADER_INIT_MEMBER(user_struct, session_keyring)
PS_HEADER_INIT_MEMBER(user_struct, uidhash_node)
PS_HEADER_INIT_MEMBER(user_struct, uid)
PS_HEADER_INIT_MEMBER(user_struct, locked_vm)
PS_HEADER_INIT_TYPE(key)
PS_HEADER_INIT_MEMBER(key, usage)
PS_HEADER_INIT_MEMBER(key, serial)
PS_HEADER_INIT_MEMBER(key, serial_node)
PS_HEADER_INIT_MEMBER(key, sem)
PS_HEADER_INIT_MEMBER(key, user)
PS_HEADER_INIT_MEMBER(key, security)
PS_HEADER_INIT_MEMBER(key, expiry)
PS_HEADER_INIT_MEMBER(key, revoked_at)
PS_HEADER_INIT_MEMBER(key, last_used_at)
PS_HEADER_INIT_MEMBER(key, uid)
PS_HEADER_INIT_MEMBER(key, gid)
PS_HEADER_INIT_MEMBER(key, perm)
PS_HEADER_INIT_MEMBER(key, quotalen)
PS_HEADER_INIT_MEMBER(key, datalen)
PS_HEADER_INIT_MEMBER(key, state)
PS_HEADER_INIT_MEMBER(key, flags)
PS_HEADER_INIT_TYPE(key_type)
PS_HEADER_INIT_TYPE(key_construction)
PS_HEADER_INIT_TYPE(key_preparsed_payload)
PS_HEADER_INIT_TYPE(key_match_data)
PS_HEADER_INIT_TYPE(keyring_index_key)
PS_HEADER_INIT_TYPE(task)
PS_HEADER_INIT_MEMBER(task, thread_info)
PS_HEADER_INIT_MEMBER(task, state)
PS_HEADER_INIT_MEMBER(task, stack)
PS_HEADER_INIT_MEMBER(task, usage)
PS_HEADER_INIT_MEMBER(task, flags)
PS_HEADER_INIT_MEMBER(task, ptrace)
PS_HEADER_INIT_MEMBER(task, wake_entry)
PS_HEADER_INIT_MEMBER(task, on_cpu)
PS_HEADER_INIT_MEMBER(task, cpu)
PS_HEADER_INIT_MEMBER(task, wakee_flips)
PS_HEADER_INIT_MEMBER(task, wakee_flip_decay_ts)
PS_HEADER_INIT_MEMBER(task, last_wakee)
PS_HEADER_INIT_MEMBER(task, wake_cpu)
PS_HEADER_INIT_MEMBER(task, on_rq)
PS_HEADER_INIT_MEMBER(task, prio)
PS_HEADER_INIT_MEMBER(task, static_prio)
PS_HEADER_INIT_MEMBER(task, normal_prio)
PS_HEADER_INIT_MEMBER(task, rt_priority)
PS_HEADER_INIT_MEMBER(task, sched_class)
PS_HEADER_INIT_MEMBER(task, se)
PS_HEADER_INIT_MEMBER(task, rt)
PS_HEADER_INIT_MEMBER(task, sched_task_group)
PS_HEADER_INIT_MEMBER(task, dl)
PS_HEADER_INIT_MEMBER(task, preempt_notifiers)
PS_HEADER_INIT_MEMBER(task, btrace_seq)
PS_HEADER_INIT_MEMBER(task, policy)
PS_HEADER_INIT_MEMBER(task, nr_cpus_allowed)
PS_HEADER_INIT_MEMBER(task, cpus_allowed)
PS_HEADER_INIT_MEMBER(task, sched_info)
PS_HEADER_INIT_MEMBER(task, tasks)
PS_HEADER_INIT_MEMBER(task, pushable_tasks)
PS_HEADER_INIT_MEMBER(task, pushable_dl_tasks)
PS_HEADER_INIT_MEMBER(task, mm)
PS_HEADER_INIT_MEMBER(task, active_mm)
PS_HEADER_INIT_MEMBER(task, vmacache)
PS_HEADER_INIT_MEMBER(task, rss_stat)
PS_HEADER_INIT_MEMBER(task, exit_state)
PS_HEADER_INIT_MEMBER(task, exit_code)
PS_HEADER_INIT_MEMBER(task, exit_signal)
PS_HEADER_INIT_MEMBER(task, pdeath_signal)
PS_HEADER_INIT_MEMBER(task, jobctl)
PS_HEADER_INIT_MEMBER(task, personality)
PS_HEADER_INIT_MEMBER(task, atomic_flags)
PS_HEADER_INIT_MEMBER(task, restart_block)
PS_HEADER_INIT_MEMBER(task, pid)
PS_HEADER_INIT_MEMBER(task, tgid)
PS_HEADER_INIT_MEMBER(task, real_parent)
PS_HEADER_INIT_MEMBER(task, parent)
PS_HEADER_INIT_MEMBER(task, children)
PS_HEADER_INIT_MEMBER(task, sibling)
PS_HEADER_INIT_MEMBER(task, group_leader)
PS_HEADER_INIT_MEMBER(task, ptraced)
PS_HEADER_INIT_MEMBER(task, ptrace_entry)
PS_HEADER_INIT_MEMBER(task, pids)
PS_HEADER_INIT_MEMBER(task, thread_group)
PS_HEADER_INIT_MEMBER(task, thread_node)
PS_HEADER_INIT_MEMBER(task, vfork_done)
PS_HEADER_INIT_MEMBER(task, set_child_tid)
PS_HEADER_INIT_MEMBER(task, clear_child_tid)
PS_HEADER_INIT_MEMBER(task, utime)
PS_HEADER_INIT_MEMBER(task, stime)
PS_HEADER_INIT_MEMBER(task, gtime)
PS_HEADER_INIT_MEMBER(task, prev_cputime)
PS_HEADER_INIT_MEMBER(task, vtime)
PS_HEADER_INIT_MEMBER(task, tick_dep_mask)
PS_HEADER_INIT_MEMBER(task, nvcsw)
PS_HEADER_INIT_MEMBER(task, nivcsw)
PS_HEADER_INIT_MEMBER(task, start_time)
PS_HEADER_INIT_MEMBER(task, real_start_time)
PS_HEADER_INIT_MEMBER(task, min_flt)
PS_HEADER_INIT_MEMBER(task, maj_flt)
PS_HEADER_INIT_MEMBER(task, cputime_expires)
PS_HEADER_INIT_MEMBER(task, cpu_timers)
PS_HEADER_INIT_MEMBER(task, ptracer_cred)
PS_HEADER_INIT_MEMBER(task, real_cred)
PS_HEADER_INIT_MEMBER(task, cred)
PS_HEADER_INIT_MEMBER(task, comm)
PS_HEADER_INIT_MEMBER(task, nameidata)
PS_HEADER_INIT_MEMBER(task, sysvsem)
PS_HEADER_INIT_MEMBER(task, sysvshm)
PS_HEADER_INIT_MEMBER(task, last_switch_count)
PS_HEADER_INIT_MEMBER(task, fs)
PS_HEADER_INIT_MEMBER(task, files)
PS_HEADER_INIT_MEMBER(task, nsproxy)
PS_HEADER_INIT_MEMBER(task, signal)
PS_HEADER_INIT_MEMBER(task, sighand)
PS_HEADER_INIT_MEMBER(task, blocked)
PS_HEADER_INIT_MEMBER(task, real_blocked)
PS_HEADER_INIT_MEMBER(task, saved_sigmask)
PS_HEADER_INIT_MEMBER(task, pending)
PS_HEADER_INIT_MEMBER(task, sas_ss_sp)
PS_HEADER_INIT_MEMBER(task, sas_ss_size)
PS_HEADER_INIT_MEMBER(task, sas_ss_flags)
PS_HEADER_INIT_MEMBER(task, thread)
PS_HEADER_INIT_TYPE(fwnode_handle)
PS_HEADER_INIT_TYPE(thread_info)
PS_HEADER_INIT_TYPE(thread_struct)
PS_HEADER_INIT_TYPE(upid)
PS_HEADER_INIT_MEMBER(upid, nr)
PS_HEADER_INIT_MEMBER(upid, ns)
PS_HEADER_INIT_MEMBER(upid, pid_chain)
PS_HEADER_INIT_TYPE(pid)
PS_HEADER_INIT_MEMBER(pid, count)
PS_HEADER_INIT_MEMBER(pid, level)
PS_HEADER_INIT_MEMBER(pid, tasks)
PS_HEADER_INIT_MEMBER(pid, rcu)
PS_HEADER_INIT_MEMBER(pid, numbers)
PS_HEADER_INIT_TYPE(pid_link)
PS_HEADER_INIT_MEMBER(pid_link, node)
PS_HEADER_INIT_MEMBER(pid_link, pid)
PS_HEADER_INIT_TYPE(pid_namespace)
PS_HEADER_INIT_TYPE(pidmap)
PS_HEADER_INIT_TYPE(mutex)
PS_HEADER_INIT_TYPE(mutex_waiter)
PS_HEADER_INIT_TYPE(kstat)
PS_HEADER_INIT_MEMBER(kstat, result_mask)
PS_HEADER_INIT_MEMBER(kstat, mode)
PS_HEADER_INIT_MEMBER(kstat, nlink)
PS_HEADER_INIT_MEMBER(kstat, blksize)
PS_HEADER_INIT_MEMBER(kstat, attributes)
PS_HEADER_INIT_MEMBER(kstat, attributes_mask)
PS_HEADER_INIT_MEMBER(kstat, ino)
PS_HEADER_INIT_MEMBER(kstat, dev)
PS_HEADER_INIT_MEMBER(kstat, rdev)
PS_HEADER_INIT_MEMBER(kstat, uid)
PS_HEADER_INIT_MEMBER(kstat, gid)
PS_HEADER_INIT_MEMBER(kstat, size)
PS_HEADER_INIT_MEMBER(kstat, atime)
PS_HEADER_INIT_MEMBER(kstat, mtime)
PS_HEADER_INIT_MEMBER(kstat, ctime)
PS_HEADER_INIT_MEMBER(kstat, btime)
PS_HEADER_INIT_MEMBER(kstat, blocks)
PS_HEADER_INIT_TYPE(kstatfs)
PS_HEADER_INIT_MEMBER(kstatfs, f_type)
PS_HEADER_INIT_MEMBER(kstatfs, f_bsize)
PS_HEADER_INIT_MEMBER(kstatfs, f_blocks)
PS_HEADER_INIT_MEMBER(kstatfs, f_bfree)
PS_HEADER_INIT_MEMBER(kstatfs, f_bavail)
PS_HEADER_INIT_MEMBER(kstatfs, f_files)
PS_HEADER_INIT_MEMBER(kstatfs, f_ffree)
PS_HEADER_INIT_MEMBER(kstatfs, f_fsid)
PS_HEADER_INIT_MEMBER(kstatfs, f_namelen)
PS_HEADER_INIT_MEMBER(kstatfs, f_frsize)
PS_HEADER_INIT_MEMBER(kstatfs, f_flags)
PS_HEADER_INIT_MEMBER(kstatfs, f_spare)
PS_HEADER_INIT_TYPE(filename)
PS_HEADER_INIT_MEMBER(filename, name)
PS_HEADER_INIT_MEMBER(filename, uptr)
PS_HEADER_INIT_MEMBER(filename, aname)
PS_HEADER_INIT_MEMBER(filename, refcnt)
PS_HEADER_INIT_TYPE(file)
PS_HEADER_INIT_MEMBER(file, f_u_fu_llist)
PS_HEADER_INIT_MEMBER(file, f_u_fu_rcuhead)
PS_HEADER_INIT_MEMBER(file, f_path)
PS_HEADER_INIT_MEMBER(file, f_inode)
PS_HEADER_INIT_MEMBER(file, f_op)
PS_HEADER_INIT_MEMBER(file, f_lock)
PS_HEADER_INIT_MEMBER(file, f_write_hint)
PS_HEADER_INIT_MEMBER(file, f_count)
PS_HEADER_INIT_MEMBER(file, f_flags)
PS_HEADER_INIT_MEMBER(file, f_mode)
PS_HEADER_INIT_MEMBER(file, f_pos_lock)
PS_HEADER_INIT_MEMBER(file, f_pos)
PS_HEADER_INIT_MEMBER(file, f_owner)
PS_HEADER_INIT_MEMBER(file, f_cred)
PS_HEADER_INIT_MEMBER(file, f_ra)
PS_HEADER_INIT_MEMBER(file, f_version)
PS_HEADER_INIT_MEMBER(file, f_security)
PS_HEADER_INIT_MEMBER(file, private_data)
PS_HEADER_INIT_MEMBER(file, f_mapping)
PS_HEADER_INIT_MEMBER(file, f_wb_err)
PS_HEADER_INIT_TYPE(path)
PS_HEADER_INIT_MEMBER(path, mnt)
PS_HEADER_INIT_MEMBER(path, dentry)
PS_HEADER_INIT_TYPE(inode)
PS_HEADER_INIT_MEMBER(inode, i_mode)
PS_HEADER_INIT_MEMBER(inode, i_opflags)
PS_HEADER_INIT_MEMBER(inode, i_uid)
PS_HEADER_INIT_MEMBER(inode, i_gid)
PS_HEADER_INIT_MEMBER(inode, i_flags)
PS_HEADER_INIT_MEMBER(inode, i_op)
PS_HEADER_INIT_MEMBER(inode, i_sb)
PS_HEADER_INIT_MEMBER(inode, i_mapping)
PS_HEADER_INIT_MEMBER(inode, i_ino)
PS_HEADER_INIT_MEMBER(inode, i_nlink)
PS_HEADER_INIT_MEMBER(inode, i_rdev)
PS_HEADER_INIT_MEMBER(inode, i_size)
PS_HEADER_INIT_MEMBER(inode, i_atime)
PS_HEADER_INIT_MEMBER(inode, i_mtime)
PS_HEADER_INIT_MEMBER(inode, i_ctime)
PS_HEADER_INIT_MEMBER(inode, i_lock)
PS_HEADER_INIT_MEMBER(inode, i_bytes)
PS_HEADER_INIT_MEMBER(inode, i_blkbits)
PS_HEADER_INIT_MEMBER(inode, i_write_hint)
PS_HEADER_INIT_MEMBER(inode, i_blocks)
PS_HEADER_INIT_MEMBER(inode, i_state)
PS_HEADER_INIT_MEMBER(inode, i_rwsem)
PS_HEADER_INIT_MEMBER(inode, dirtied_when)
PS_HEADER_INIT_MEMBER(inode, dirtied_time_when)
PS_HEADER_INIT_MEMBER(inode, i_hash)
PS_HEADER_INIT_MEMBER(inode, i_io_list)
PS_HEADER_INIT_MEMBER(inode, i_lru)
PS_HEADER_INIT_MEMBER(inode, i_sb_list)
PS_HEADER_INIT_MEMBER(inode, i_wb_list)
PS_HEADER_INIT_MEMBER(inode, i_dentry)
PS_HEADER_INIT_MEMBER(inode, i_rcu)
PS_HEADER_INIT_MEMBER(inode, i_version)
PS_HEADER_INIT_MEMBER(inode, i_count)
PS_HEADER_INIT_MEMBER(inode, i_dio_count)
PS_HEADER_INIT_MEMBER(inode, i_writecount)
PS_HEADER_INIT_MEMBER(inode, i_fop)
PS_HEADER_INIT_MEMBER(inode, i_flctx)
PS_HEADER_INIT_MEMBER(inode, i_data)
PS_HEADER_INIT_MEMBER(inode, i_devices)
PS_HEADER_INIT_MEMBER(inode, i_pipe)
PS_HEADER_INIT_MEMBER(inode, i_bdev)
PS_HEADER_INIT_MEMBER(inode, i_cdev)
PS_HEADER_INIT_MEMBER(inode, i_link)
PS_HEADER_INIT_MEMBER(inode, i_dir_seq)
PS_HEADER_INIT_MEMBER(inode, i_generation)
PS_HEADER_INIT_MEMBER(inode, i_private)
PS_HEADER_INIT_TYPE(iattr)
PS_HEADER_INIT_MEMBER(iattr, ia_valid)
PS_HEADER_INIT_MEMBER(iattr, ia_mode)
PS_HEADER_INIT_MEMBER(iattr, ia_uid)
PS_HEADER_INIT_MEMBER(iattr, ia_gid)
PS_HEADER_INIT_MEMBER(iattr, ia_size)
PS_HEADER_INIT_MEMBER(iattr, ia_atime)
PS_HEADER_INIT_MEMBER(iattr, ia_mtime)
PS_HEADER_INIT_MEMBER(iattr, ia_ctime)
PS_HEADER_INIT_MEMBER(iattr, ia_file)
PS_HEADER_INIT_TYPE(block_device)
PS_HEADER_INIT_MEMBER(block_device, bd_dev)
PS_HEADER_INIT_MEMBER(block_device, bd_openers)
PS_HEADER_INIT_MEMBER(block_device, bd_inode)
PS_HEADER_INIT_MEMBER(block_device, bd_super)
PS_HEADER_INIT_MEMBER(block_device, bd_mutex)
PS_HEADER_INIT_MEMBER(block_device, bd_claiming)
PS_HEADER_INIT_MEMBER(block_device, bd_holder)
PS_HEADER_INIT_MEMBER(block_device, bd_holders)
PS_HEADER_INIT_MEMBER(block_device, bd_write_holder)
PS_HEADER_INIT_MEMBER(block_device, bd_holder_disks)
PS_HEADER_INIT_MEMBER(block_device, bd_contains)
PS_HEADER_INIT_MEMBER(block_device, bd_block_size)
PS_HEADER_INIT_MEMBER(block_device, bd_partno)
PS_HEADER_INIT_MEMBER(block_device, bd_part)
PS_HEADER_INIT_MEMBER(block_device, bd_part_count)
PS_HEADER_INIT_MEMBER(block_device, bd_invalidated)
PS_HEADER_INIT_MEMBER(block_device, bd_disk)
PS_HEADER_INIT_MEMBER(block_device, bd_queue)
PS_HEADER_INIT_MEMBER(block_device, bd_bdi)
PS_HEADER_INIT_MEMBER(block_device, bd_list)
PS_HEADER_INIT_MEMBER(block_device, bd_private)
PS_HEADER_INIT_MEMBER(block_device, bd_fsfreeze_count)
PS_HEADER_INIT_MEMBER(block_device, bd_fsfreeze_mutex)
PS_HEADER_INIT_TYPE(fown_struct)
PS_HEADER_INIT_MEMBER(fown_struct, lock)
PS_HEADER_INIT_MEMBER(fown_struct, pid)
PS_HEADER_INIT_MEMBER(fown_struct, pid_type)
PS_HEADER_INIT_MEMBER(fown_struct, uid)
PS_HEADER_INIT_MEMBER(fown_struct, euid)
PS_HEADER_INIT_MEMBER(fown_struct, signum)
PS_HEADER_INIT_TYPE(fasync_struct )
PS_HEADER_INIT_TYPE(sb_writers)
PS_HEADER_INIT_TYPE(super_block)
PS_HEADER_INIT_TYPE(dir_context)
PS_HEADER_INIT_MEMBER(dir_context, actor)
PS_HEADER_INIT_MEMBER(dir_context, pos)
PS_HEADER_INIT_TYPE(file_system_type)
PS_HEADER_INIT_MEMBER(file_system_type, name)
PS_HEADER_INIT_MEMBER(file_system_type, mount)
PS_HEADER_INIT_MEMBER(file_system_type, fs_flags)
PS_HEADER_INIT_MEMBER(file_system_type, kill_sb)
PS_HEADER_INIT_MEMBER(file_system_type, owner)
PS_HEADER_INIT_MEMBER(file_system_type, next)
PS_HEADER_INIT_MEMBER(file_system_type, fs_supers)
PS_HEADER_INIT_MEMBER(file_system_type, s_lock_key)
PS_HEADER_INIT_MEMBER(file_system_type, s_umount_key)
PS_HEADER_INIT_MEMBER(file_system_type, s_vfs_rename_key)
PS_HEADER_INIT_MEMBER(file_system_type, s_writers_key)
PS_HEADER_INIT_MEMBER(file_system_type, i_lock_key)
PS_HEADER_INIT_MEMBER(file_system_type, i_mutex_key)
PS_HEADER_INIT_MEMBER(file_system_type, i_mutex_dir_key)
PS_HEADER_INIT_TYPE(vfsmount)
PS_HEADER_INIT_MEMBER(vfsmount, mnt_root)
PS_HEADER_INIT_MEMBER(vfsmount, mnt_sb)
PS_HEADER_INIT_MEMBER(vfsmount, mnt_flags)
PS_HEADER_INIT_TYPE(fs_struct)
PS_HEADER_INIT_MEMBER(fs_struct, users)
PS_HEADER_INIT_MEMBER(fs_struct, lock)
PS_HEADER_INIT_MEMBER(fs_struct, seq)
PS_HEADER_INIT_MEMBER(fs_struct, umask)
PS_HEADER_INIT_MEMBER(fs_struct, in_exec)
PS_HEADER_INIT_MEMBER(fs_struct, root)
PS_HEADER_INIT_MEMBER(fs_struct, pwd)
PS_HEADER_INIT_TYPE(file_ra_state)
PS_HEADER_INIT_MEMBER(file_ra_state, start)
PS_HEADER_INIT_MEMBER(file_ra_state, size)
PS_HEADER_INIT_MEMBER(file_ra_state, async_size)
PS_HEADER_INIT_MEMBER(file_ra_state, ra_pages)
PS_HEADER_INIT_MEMBER(file_ra_state, mmap_miss)
PS_HEADER_INIT_MEMBER(file_ra_state, prev_pos)
PS_HEADER_INIT_TYPE(file_lock)
PS_HEADER_INIT_MEMBER(file_lock, fl_next)
PS_HEADER_INIT_MEMBER(file_lock, fl_list)
PS_HEADER_INIT_MEMBER(file_lock, fl_link)
PS_HEADER_INIT_MEMBER(file_lock, fl_block)
PS_HEADER_INIT_MEMBER(file_lock, fl_owner)
PS_HEADER_INIT_MEMBER(file_lock, fl_flags)
PS_HEADER_INIT_MEMBER(file_lock, fl_type)
PS_HEADER_INIT_MEMBER(file_lock, fl_pid)
PS_HEADER_INIT_MEMBER(file_lock, fl_link_cpu)
PS_HEADER_INIT_MEMBER(file_lock, fl_wait)
PS_HEADER_INIT_MEMBER(file_lock, fl_file)
PS_HEADER_INIT_MEMBER(file_lock, fl_start)
PS_HEADER_INIT_MEMBER(file_lock, fl_end)
PS_HEADER_INIT_MEMBER(file_lock, fl_fasync)
PS_HEADER_INIT_MEMBER(file_lock, fl_break_time)
PS_HEADER_INIT_MEMBER(file_lock, fl_downgrade_time)
PS_HEADER_INIT_MEMBER(file_lock, fl_ops)
PS_HEADER_INIT_MEMBER(file_lock, fl_lmops)
PS_HEADER_INIT_TYPE(file_lock_operations)
PS_HEADER_INIT_MEMBER(file_lock_operations, fl_release_private)
PS_HEADER_INIT_TYPE(lock_manager)
PS_HEADER_INIT_MEMBER(lock_manager, list)
PS_HEADER_INIT_MEMBER(lock_manager, block_opens)
PS_HEADER_INIT_TYPE(lock_manager_operations)
PS_HEADER_INIT_MEMBER(lock_manager_operations, lm_compare_owner)
PS_HEADER_INIT_MEMBER(lock_manager_operations, lm_owner_key)
PS_HEADER_INIT_MEMBER(lock_manager_operations, lm_get_owner)
PS_HEADER_INIT_MEMBER(lock_manager_operations, lm_put_owner)
PS_HEADER_INIT_MEMBER(lock_manager_operations, lm_notify)
PS_HEADER_INIT_MEMBER(lock_manager_operations, lm_grant)
PS_HEADER_INIT_MEMBER(lock_manager_operations, lm_break)
PS_HEADER_INIT_MEMBER(lock_manager_operations, lm_change)
PS_HEADER_INIT_MEMBER(lock_manager_operations, lm_setup)
PS_HEADER_INIT_TYPE(file_operations)
PS_HEADER_INIT_MEMBER(file_operations, owner)
PS_HEADER_INIT_MEMBER(file_operations, llseek)
PS_HEADER_INIT_MEMBER(file_operations, read)
PS_HEADER_INIT_MEMBER(file_operations, write)
PS_HEADER_INIT_MEMBER(file_operations, read_iter)
PS_HEADER_INIT_MEMBER(file_operations, write_iter)
PS_HEADER_INIT_MEMBER(file_operations, iterate)
PS_HEADER_INIT_MEMBER(file_operations, iterate_shared)
PS_HEADER_INIT_MEMBER(file_operations, poll)
PS_HEADER_INIT_MEMBER(file_operations, unlocked_ioctl)
PS_HEADER_INIT_MEMBER(file_operations, compat_ioctl)
PS_HEADER_INIT_MEMBER(file_operations, mmap)
PS_HEADER_INIT_MEMBER(file_operations, open)
PS_HEADER_INIT_MEMBER(file_operations, flush)
PS_HEADER_INIT_MEMBER(file_operations, release)
PS_HEADER_INIT_MEMBER(file_operations, fsync)
PS_HEADER_INIT_MEMBER(file_operations, fasync)
PS_HEADER_INIT_MEMBER(file_operations, lock)
PS_HEADER_INIT_MEMBER(file_operations, sendpage)
PS_HEADER_INIT_MEMBER(file_operations, get_unmapped_area)
PS_HEADER_INIT_MEMBER(file_operations, check_flags)
PS_HEADER_INIT_MEMBER(file_operations, flock)
PS_HEADER_INIT_MEMBER(file_operations, splice_write)
PS_HEADER_INIT_MEMBER(file_operations, splice_read)
PS_HEADER_INIT_MEMBER(file_operations, setlease)
PS_HEADER_INIT_MEMBER(file_operations, fallocate)
PS_HEADER_INIT_MEMBER(file_operations, show_fdinfo)
PS_HEADER_INIT_MEMBER(file_operations, copy_file_range)
PS_HEADER_INIT_MEMBER(file_operations, clone_file_range)
PS_HEADER_INIT_MEMBER(file_operations, dedupe_file_range)
PS_HEADER_INIT_TYPE(inode_operations)
PS_HEADER_INIT_MEMBER(inode_operations, lookup)
PS_HEADER_INIT_MEMBER(inode_operations, get_link)
PS_HEADER_INIT_MEMBER(inode_operations, permission)
PS_HEADER_INIT_MEMBER(inode_operations, get_acl)
PS_HEADER_INIT_MEMBER(inode_operations, readlink)
PS_HEADER_INIT_MEMBER(inode_operations, create)
PS_HEADER_INIT_MEMBER(inode_operations, link)
PS_HEADER_INIT_MEMBER(inode_operations, unlink)
PS_HEADER_INIT_MEMBER(inode_operations, symlink)
PS_HEADER_INIT_MEMBER(inode_operations, mkdir)
PS_HEADER_INIT_MEMBER(inode_operations, rmdir)
PS_HEADER_INIT_MEMBER(inode_operations, mknod)
PS_HEADER_INIT_MEMBER(inode_operations, rename)
PS_HEADER_INIT_MEMBER(inode_operations, setattr)
PS_HEADER_INIT_MEMBER(inode_operations, getattr)
PS_HEADER_INIT_MEMBER(inode_operations, listxattr)
PS_HEADER_INIT_MEMBER(inode_operations, fiemap)
PS_HEADER_INIT_MEMBER(inode_operations, update_time)
PS_HEADER_INIT_MEMBER(inode_operations, atomic_open)
PS_HEADER_INIT_MEMBER(inode_operations, tmpfile)
PS_HEADER_INIT_MEMBER(inode_operations, set_acl)
PS_HEADER_INIT_TYPE(super_operations)
PS_HEADER_INIT_MEMBER(super_operations, alloc_inode)
PS_HEADER_INIT_MEMBER(super_operations, destroy_inode)
PS_HEADER_INIT_MEMBER(super_operations, dirty_inode)
PS_HEADER_INIT_MEMBER(super_operations, write_inode)
PS_HEADER_INIT_MEMBER(super_operations, drop_inode)
PS_HEADER_INIT_MEMBER(super_operations, evict_inode)
PS_HEADER_INIT_MEMBER(super_operations, put_super)
PS_HEADER_INIT_MEMBER(super_operations, sync_fs)
PS_HEADER_INIT_MEMBER(super_operations, freeze_super)
PS_HEADER_INIT_MEMBER(super_operations, freeze_fs)
PS_HEADER_INIT_MEMBER(super_operations, thaw_super)
PS_HEADER_INIT_MEMBER(super_operations, unfreeze_fs)
PS_HEADER_INIT_MEMBER(super_operations, statfs)
PS_HEADER_INIT_MEMBER(super_operations, remount_fs)
PS_HEADER_INIT_MEMBER(super_operations, umount_begin)
PS_HEADER_INIT_MEMBER(super_operations, show_options)
PS_HEADER_INIT_MEMBER(super_operations, show_devname)
PS_HEADER_INIT_MEMBER(super_operations, show_path)
PS_HEADER_INIT_MEMBER(super_operations, show_stats)
PS_HEADER_INIT_MEMBER(super_operations, nr_cached_objects)
PS_HEADER_INIT_MEMBER(super_operations, free_cached_objects)
PS_HEADER_INIT_TYPE(address_space)
PS_HEADER_INIT_MEMBER(address_space, host)
PS_HEADER_INIT_MEMBER(address_space, page_tree)
PS_HEADER_INIT_MEMBER(address_space, tree_lock)
PS_HEADER_INIT_MEMBER(address_space, i_mmap_writable)
PS_HEADER_INIT_MEMBER(address_space, i_mmap)
PS_HEADER_INIT_MEMBER(address_space, i_mmap_rwsem)
PS_HEADER_INIT_MEMBER(address_space, nrpages)
PS_HEADER_INIT_MEMBER(address_space, nrexceptional)
PS_HEADER_INIT_MEMBER(address_space, a_ops)
PS_HEADER_INIT_MEMBER(address_space, flags)
PS_HEADER_INIT_MEMBER(address_space, private_lock)
PS_HEADER_INIT_MEMBER(address_space, gfp_mask)
PS_HEADER_INIT_MEMBER(address_space, private_list)
PS_HEADER_INIT_MEMBER(address_space, private_data)
PS_HEADER_INIT_MEMBER(address_space, wb_err)
PS_HEADER_INIT_TYPE(address_space_operations)
PS_HEADER_INIT_MEMBER(address_space_operations, writepage)
PS_HEADER_INIT_MEMBER(address_space_operations, readpage)
PS_HEADER_INIT_MEMBER(address_space_operations, writepages)
PS_HEADER_INIT_MEMBER(address_space_operations, set_page_dirty)
PS_HEADER_INIT_MEMBER(address_space_operations, readpages)
PS_HEADER_INIT_MEMBER(address_space_operations, write_begin)
PS_HEADER_INIT_MEMBER(address_space_operations, write_end)
PS_HEADER_INIT_MEMBER(address_space_operations, bmap)
PS_HEADER_INIT_MEMBER(address_space_operations, invalidatepage)
PS_HEADER_INIT_MEMBER(address_space_operations, releasepage)
PS_HEADER_INIT_MEMBER(address_space_operations, freepage)
PS_HEADER_INIT_MEMBER(address_space_operations, direct_IO)
PS_HEADER_INIT_MEMBER(address_space_operations, migratepage)
PS_HEADER_INIT_MEMBER(address_space_operations, isolate_page)
PS_HEADER_INIT_MEMBER(address_space_operations, putback_page)
PS_HEADER_INIT_MEMBER(address_space_operations, launder_page)
PS_HEADER_INIT_MEMBER(address_space_operations, is_partially_uptodate)
PS_HEADER_INIT_MEMBER(address_space_operations, is_dirty_writeback)
PS_HEADER_INIT_MEMBER(address_space_operations, error_remove_page)
PS_HEADER_INIT_MEMBER(address_space_operations, swap_activate)
PS_HEADER_INIT_MEMBER(address_space_operations, swap_deactivate)
PS_HEADER_INIT_TYPE(seq_file)
PS_HEADER_INIT_MEMBER(seq_file, buf)
PS_HEADER_INIT_MEMBER(seq_file, size)
PS_HEADER_INIT_MEMBER(seq_file, from)
PS_HEADER_INIT_MEMBER(seq_file, count)
PS_HEADER_INIT_MEMBER(seq_file, pad_until)
PS_HEADER_INIT_MEMBER(seq_file, index)
PS_HEADER_INIT_MEMBER(seq_file, read_pos)
PS_HEADER_INIT_MEMBER(seq_file, version)
PS_HEADER_INIT_MEMBER(seq_file, lock)
PS_HEADER_INIT_MEMBER(seq_file, op)
PS_HEADER_INIT_MEMBER(seq_file, poll_event)
PS_HEADER_INIT_MEMBER(seq_file, file)
PS_HEADER_INIT_MEMBER(seq_file, private)
PS_HEADER_INIT_TYPE(seq_operations)
PS_HEADER_INIT_MEMBER(seq_operations, start)
PS_HEADER_INIT_MEMBER(seq_operations, stop)
PS_HEADER_INIT_MEMBER(seq_operations, next)
PS_HEADER_INIT_MEMBER(seq_operations, show)
PS_HEADER_INIT_TYPE(core_state)
PS_HEADER_INIT_MEMBER(core_state, dumper)
PS_HEADER_INIT_MEMBER(core_state, nr_threads)
PS_HEADER_INIT_MEMBER(core_state, startup)
PS_HEADER_INIT_TYPE(core_thread)
PS_HEADER_INIT_MEMBER(core_thread, task)
PS_HEADER_INIT_MEMBER(core_thread, next)
PS_HEADER_INIT_TYPE(mm_struct)
PS_HEADER_INIT_MEMBER(mm_struct, mmap)
PS_HEADER_INIT_MEMBER(mm_struct, mm_rb)
PS_HEADER_INIT_MEMBER(mm_struct, vmacache_seqnum)
PS_HEADER_INIT_MEMBER(mm_struct, get_unmapped_area)
PS_HEADER_INIT_MEMBER(mm_struct, mmap_base)
PS_HEADER_INIT_MEMBER(mm_struct, mmap_legacy_base)
PS_HEADER_INIT_MEMBER(mm_struct, task_size)
PS_HEADER_INIT_MEMBER(mm_struct, highest_vm_end)
PS_HEADER_INIT_MEMBER(mm_struct, pgd)
PS_HEADER_INIT_MEMBER(mm_struct, mm_users)
PS_HEADER_INIT_MEMBER(mm_struct, mm_count)
PS_HEADER_INIT_MEMBER(mm_struct, map_count)
PS_HEADER_INIT_MEMBER(mm_struct, page_table_lock)
PS_HEADER_INIT_MEMBER(mm_struct, mmap_sem)
PS_HEADER_INIT_MEMBER(mm_struct, mmlist)
PS_HEADER_INIT_MEMBER(mm_struct, hiwater_rss)
PS_HEADER_INIT_MEMBER(mm_struct, hiwater_vm)
PS_HEADER_INIT_MEMBER(mm_struct, total_vm)
PS_HEADER_INIT_MEMBER(mm_struct, locked_vm)
PS_HEADER_INIT_MEMBER(mm_struct, pinned_vm)
PS_HEADER_INIT_MEMBER(mm_struct, data_vm)
PS_HEADER_INIT_MEMBER(mm_struct, exec_vm)
PS_HEADER_INIT_MEMBER(mm_struct, stack_vm)
PS_HEADER_INIT_MEMBER(mm_struct, def_flags)
PS_HEADER_INIT_MEMBER(mm_struct, start_code)
PS_HEADER_INIT_MEMBER(mm_struct, end_code)
PS_HEADER_INIT_MEMBER(mm_struct, start_data)
PS_HEADER_INIT_MEMBER(mm_struct, end_data)
PS_HEADER_INIT_MEMBER(mm_struct, start_brk)
PS_HEADER_INIT_MEMBER(mm_struct, brk)
PS_HEADER_INIT_MEMBER(mm_struct, start_stack)
PS_HEADER_INIT_MEMBER(mm_struct, arg_start)
PS_HEADER_INIT_MEMBER(mm_struct, arg_end)
PS_HEADER_INIT_MEMBER(mm_struct, env_start)
PS_HEADER_INIT_MEMBER(mm_struct, env_end)
PS_HEADER_INIT_MEMBER(mm_struct, saved_auxv)
PS_HEADER_INIT_MEMBER(mm_struct, rss_stat)
PS_HEADER_INIT_MEMBER(mm_struct, binfmt)
PS_HEADER_INIT_MEMBER(mm_struct, cpu_vm_mask_var)
PS_HEADER_INIT_MEMBER(mm_struct, context)
PS_HEADER_INIT_MEMBER(mm_struct, flags)
PS_HEADER_INIT_MEMBER(mm_struct, core_state)
PS_HEADER_INIT_MEMBER(mm_struct, user_ns)
PS_HEADER_INIT_MEMBER(mm_struct, exe_file)
PS_HEADER_INIT_MEMBER(mm_struct, tlb_flush_pending)
PS_HEADER_INIT_MEMBER(mm_struct, uprobes_state)
PS_HEADER_INIT_MEMBER(mm_struct, async_put_work)
PS_HEADER_INIT_TYPE(vm_area_struct)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_start)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_end)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_next)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_prev)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_rb)
PS_HEADER_INIT_MEMBER(vm_area_struct, rb_subtree_gap)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_mm)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_page_prot)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_flags)
PS_HEADER_INIT_MEMBER(vm_area_struct, shared_rb)
PS_HEADER_INIT_MEMBER(vm_area_struct, shared_rb_subtree_last)
PS_HEADER_INIT_MEMBER(vm_area_struct, anon_vma_chain)
PS_HEADER_INIT_MEMBER(vm_area_struct, anon_vma)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_ops)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_pgoff)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_file)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_private_data)
PS_HEADER_INIT_MEMBER(vm_area_struct, swap_readahead_info)
PS_HEADER_INIT_MEMBER(vm_area_struct, vm_userfaultfd_ctx)
PS_HEADER_INIT_TYPE(vm_region)
PS_HEADER_INIT_MEMBER(vm_region, vm_rb)
PS_HEADER_INIT_MEMBER(vm_region, vm_flags)
PS_HEADER_INIT_MEMBER(vm_region, vm_start)
PS_HEADER_INIT_MEMBER(vm_region, vm_end)
PS_HEADER_INIT_MEMBER(vm_region, vm_top)
PS_HEADER_INIT_MEMBER(vm_region, vm_pgoff)
PS_HEADER_INIT_MEMBER(vm_region, vm_file)
PS_HEADER_INIT_MEMBER(vm_region, vm_usage)
PS_HEADER_INIT_TYPE(vm_fault)
PS_HEADER_INIT_MEMBER(vm_fault, vma)
PS_HEADER_INIT_MEMBER(vm_fault, flags)
PS_HEADER_INIT_MEMBER(vm_fault, gfp_mask)
PS_HEADER_INIT_MEMBER(vm_fault, pgoff)
PS_HEADER_INIT_MEMBER(vm_fault, address)
PS_HEADER_INIT_MEMBER(vm_fault, pmd)
PS_HEADER_INIT_MEMBER(vm_fault, pud)
PS_HEADER_INIT_MEMBER(vm_fault, orig_pte)
PS_HEADER_INIT_MEMBER(vm_fault, cow_page)
PS_HEADER_INIT_MEMBER(vm_fault, memcg)
PS_HEADER_INIT_MEMBER(vm_fault, page)
PS_HEADER_INIT_MEMBER(vm_fault, pte)
PS_HEADER_INIT_MEMBER(vm_fault, ptl)
PS_HEADER_INIT_MEMBER(vm_fault, prealloc_pte)
PS_HEADER_INIT_TYPE(vm_operations_struct)
PS_HEADER_INIT_MEMBER(vm_operations_struct, open)
PS_HEADER_INIT_MEMBER(vm_operations_struct, close)
PS_HEADER_INIT_MEMBER(vm_operations_struct, split)
PS_HEADER_INIT_MEMBER(vm_operations_struct, mremap)
PS_HEADER_INIT_MEMBER(vm_operations_struct, fault)
PS_HEADER_INIT_MEMBER(vm_operations_struct, huge_fault)
PS_HEADER_INIT_MEMBER(vm_operations_struct, map_pages)
PS_HEADER_INIT_MEMBER(vm_operations_struct, page_mkwrite)
PS_HEADER_INIT_MEMBER(vm_operations_struct, pfn_mkwrite)
PS_HEADER_INIT_MEMBER(vm_operations_struct, access)
PS_HEADER_INIT_MEMBER(vm_operations_struct, name)
PS_HEADER_INIT_MEMBER(vm_operations_struct, find_special_page)
PS_HEADER_INIT_TYPE(vm_special_mapping)
PS_HEADER_INIT_TYPE(page)
PS_HEADER_INIT_MEMBER(page, flags)
PS_HEADER_INIT_TYPE(scatterlist)
PS_HEADER_INIT_MEMBER(scatterlist, page_link)
PS_HEADER_INIT_MEMBER(scatterlist, offset)
PS_HEADER_INIT_MEMBER(scatterlist, length)
PS_HEADER_INIT_MEMBER(scatterlist, dma_address)
PS_HEADER_INIT_TYPE(usb_interface_descriptor)
PS_HEADER_INIT_TYPE(usb_host_endpoint)
PS_HEADER_INIT_TYPE(usb_bus)
PS_HEADER_INIT_TYPE(usb_interface)
PS_HEADER_INIT_TYPE(usb_host_interface)
PS_HEADER_INIT_TYPE(usb_device)
PS_HEADER_INIT_TYPE(usb_driver)
PS_HEADER_INIT_TYPE(usb_device_driver)
PS_HEADER_INIT_TYPE(usb_anchor)
PS_HEADER_INIT_TYPE(usb_iso_packet_descriptor)
PS_HEADER_INIT_TYPE(usb_sg_request)
PS_HEADER_INIT_TYPE(usb_interface_cache)
PS_HEADER_INIT_TYPE(usb_host_config)
PS_HEADER_INIT_TYPE(usb_host_bos)
PS_HEADER_INIT_TYPE(urb)
PS_HEADER_INIT_TYPE(usb_ss_ep_comp_descriptor)
PS_HEADER_INIT_TYPE(usb_ssp_isoc_ep_comp_descriptor)
PS_HEADER_INIT_TYPE(usb_interface_assoc_descriptor)
PS_HEADER_INIT_TYPE(usb_endpoint_descriptor)
PS_HEADER_INIT_TYPE(usb_config_descriptor)
PS_HEADER_INIT_TYPE(notifier_block)
PS_HEADER_INIT_TYPE(atomic_notifier_head)
PS_HEADER_INIT_MEMBER(atomic_notifier_head, head)
PS_HEADER_INIT_MEMBER(atomic_notifier_head, lock)
PS_HEADER_INIT_TYPE(blocking_notifier_head)
PS_HEADER_INIT_MEMBER(blocking_notifier_head, rwsem)
PS_HEADER_INIT_MEMBER(blocking_notifier_head, head)
PS_HEADER_INIT_TYPE(raw_notifier_head)
PS_HEADER_INIT_MEMBER(raw_notifier_head, head)
PS_HEADER_INIT_TYPE(srcu_notifier_head)
PS_HEADER_INIT_TYPE(pci_driver)
PS_HEADER_INIT_MEMBER(pci_driver, node)
PS_HEADER_INIT_MEMBER(pci_driver, name)
PS_HEADER_INIT_MEMBER(pci_driver, id_table)
PS_HEADER_INIT_MEMBER(pci_driver, probe)
PS_HEADER_INIT_MEMBER(pci_driver, remove)
PS_HEADER_INIT_MEMBER(pci_driver, suspend)
PS_HEADER_INIT_MEMBER(pci_driver, suspend_late)
PS_HEADER_INIT_MEMBER(pci_driver, resume_early)
PS_HEADER_INIT_MEMBER(pci_driver, resume)
PS_HEADER_INIT_MEMBER(pci_driver, shutdown)
PS_HEADER_INIT_MEMBER(pci_driver, sriov_configure)
PS_HEADER_INIT_MEMBER(pci_driver, err_handler)
PS_HEADER_INIT_MEMBER(pci_driver, groups)
PS_HEADER_INIT_MEMBER(pci_driver, driver)
PS_HEADER_INIT_MEMBER(pci_driver, dynids)
PS_HEADER_INIT_TYPE(pci_device_id)
PS_HEADER_INIT_MEMBER(pci_device_id, vendor)
PS_HEADER_INIT_MEMBER(pci_device_id, device)
PS_HEADER_INIT_MEMBER(pci_device_id, subvendor)
PS_HEADER_INIT_MEMBER(pci_device_id, subdevice)
PS_HEADER_INIT_MEMBER(pci_device_id, class)
PS_HEADER_INIT_MEMBER(pci_device_id, class_mask)
PS_HEADER_INIT_MEMBER(pci_device_id, driver_data)
PS_HEADER_INIT_TYPE(pci_bus)
PS_HEADER_INIT_MEMBER(pci_bus, node)
PS_HEADER_INIT_MEMBER(pci_bus, parent)
PS_HEADER_INIT_MEMBER(pci_bus, children)
PS_HEADER_INIT_MEMBER(pci_bus, devices)
PS_HEADER_INIT_MEMBER(pci_bus, self)
PS_HEADER_INIT_MEMBER(pci_bus, slots)
PS_HEADER_INIT_MEMBER(pci_bus, resource)
PS_HEADER_INIT_MEMBER(pci_bus, resources)
PS_HEADER_INIT_MEMBER(pci_bus, busn_res)
PS_HEADER_INIT_MEMBER(pci_bus, ops)
PS_HEADER_INIT_MEMBER(pci_bus, msi)
PS_HEADER_INIT_MEMBER(pci_bus, sysdata)
PS_HEADER_INIT_MEMBER(pci_bus, procdir)
PS_HEADER_INIT_MEMBER(pci_bus, number)
PS_HEADER_INIT_MEMBER(pci_bus, primary)
PS_HEADER_INIT_MEMBER(pci_bus, max_bus_speed)
PS_HEADER_INIT_MEMBER(pci_bus, cur_bus_speed)
PS_HEADER_INIT_MEMBER(pci_bus, name)
PS_HEADER_INIT_MEMBER(pci_bus, bridge_ctl)
PS_HEADER_INIT_MEMBER(pci_bus, bus_flags)
PS_HEADER_INIT_MEMBER(pci_bus, bridge)
PS_HEADER_INIT_MEMBER(pci_bus, dev)
PS_HEADER_INIT_MEMBER(pci_bus, legacy_io)
PS_HEADER_INIT_MEMBER(pci_bus, legacy_mem)
PS_HEADER_INIT_TYPE(pci_slot)
PS_HEADER_INIT_MEMBER(pci_slot, bus)
PS_HEADER_INIT_MEMBER(pci_slot, list)
PS_HEADER_INIT_MEMBER(pci_slot, hotplug)
PS_HEADER_INIT_MEMBER(pci_slot, number)
PS_HEADER_INIT_MEMBER(pci_slot, kobj)
PS_HEADER_INIT_TYPE(pci_dev)
PS_HEADER_INIT_MEMBER(pci_dev, bus_list)
PS_HEADER_INIT_MEMBER(pci_dev, bus)
PS_HEADER_INIT_MEMBER(pci_dev, subordinate)
PS_HEADER_INIT_MEMBER(pci_dev, sysdata)
PS_HEADER_INIT_MEMBER(pci_dev, procent)
PS_HEADER_INIT_MEMBER(pci_dev, slot)
PS_HEADER_INIT_MEMBER(pci_dev, devfn)
PS_HEADER_INIT_MEMBER(pci_dev, vendor)
PS_HEADER_INIT_MEMBER(pci_dev, device)
PS_HEADER_INIT_MEMBER(pci_dev, subsystem_vendor)
PS_HEADER_INIT_MEMBER(pci_dev, subsystem_device)
PS_HEADER_INIT_MEMBER(pci_dev, class)
PS_HEADER_INIT_MEMBER(pci_dev, revision)
PS_HEADER_INIT_MEMBER(pci_dev, hdr_type)
PS_HEADER_INIT_MEMBER(pci_dev, aer_cap)
PS_HEADER_INIT_MEMBER(pci_dev, pcie_cap)
PS_HEADER_INIT_MEMBER(pci_dev, msi_cap)
PS_HEADER_INIT_MEMBER(pci_dev, msix_cap)
PS_HEADER_INIT_MEMBER(pci_dev, rom_base_reg)
PS_HEADER_INIT_MEMBER(pci_dev, pin)
PS_HEADER_INIT_MEMBER(pci_dev, pcie_flags_reg)
PS_HEADER_INIT_MEMBER(pci_dev, dma_alias_mask)
PS_HEADER_INIT_MEMBER(pci_dev, driver)
PS_HEADER_INIT_MEMBER(pci_dev, dma_mask)
PS_HEADER_INIT_MEMBER(pci_dev, dma_parms)
PS_HEADER_INIT_MEMBER(pci_dev, current_state)
PS_HEADER_INIT_MEMBER(pci_dev, pm_cap)
PS_HEADER_INIT_MEMBER(pci_dev, d3_delay)
PS_HEADER_INIT_MEMBER(pci_dev, d3cold_delay)
PS_HEADER_INIT_MEMBER(pci_dev, link_state)
PS_HEADER_INIT_MEMBER(pci_dev, error_state)
PS_HEADER_INIT_MEMBER(pci_dev, dev)
PS_HEADER_INIT_MEMBER(pci_dev, cfg_size)
PS_HEADER_INIT_MEMBER(pci_dev, irq)
PS_HEADER_INIT_MEMBER(pci_dev, resource)
PS_HEADER_INIT_MEMBER(pci_dev, match_driver)
PS_HEADER_INIT_MEMBER(pci_dev, dev_flags)
PS_HEADER_INIT_MEMBER(pci_dev, enable_cnt)
PS_HEADER_INIT_MEMBER(pci_dev, saved_config_space)
PS_HEADER_INIT_MEMBER(pci_dev, saved_cap_space)
PS_HEADER_INIT_MEMBER(pci_dev, rom_attr)
PS_HEADER_INIT_MEMBER(pci_dev, rom_attr_enabled)
PS_HEADER_INIT_MEMBER(pci_dev, res_attr)
PS_HEADER_INIT_MEMBER(pci_dev, res_attr_wc)
PS_HEADER_INIT_MEMBER(pci_dev, msi_irq_groups)
PS_HEADER_INIT_MEMBER(pci_dev, vpd)
PS_HEADER_INIT_MEMBER(pci_dev, ats_cap)
PS_HEADER_INIT_MEMBER(pci_dev, ats_stu)
PS_HEADER_INIT_MEMBER(pci_dev, ats_ref_cnt)
PS_HEADER_INIT_MEMBER(pci_dev, pri_reqs_alloc)
PS_HEADER_INIT_MEMBER(pci_dev, pasid_features)
PS_HEADER_INIT_MEMBER(pci_dev, rom)
PS_HEADER_INIT_MEMBER(pci_dev, romlen)
PS_HEADER_INIT_MEMBER(pci_dev, driver_override)
PS_HEADER_INIT_MEMBER(pci_dev, priv_flags)
PS_HEADER_INIT_TYPE(pci_error_handlers)
PS_HEADER_INIT_MEMBER(pci_error_handlers, error_detected)
PS_HEADER_INIT_MEMBER(pci_error_handlers, mmio_enabled)
PS_HEADER_INIT_MEMBER(pci_error_handlers, slot_reset)
PS_HEADER_INIT_MEMBER(pci_error_handlers, reset_prepare)
PS_HEADER_INIT_MEMBER(pci_error_handlers, reset_done)
PS_HEADER_INIT_MEMBER(pci_error_handlers, resume)
PS_HEADER_INIT_TYPE(pci_dynids)
PS_HEADER_INIT_MEMBER(pci_dynids, lock)
PS_HEADER_INIT_MEMBER(pci_dynids, list)
PS_HEADER_INIT_TYPE(pci_bus_region)
PS_HEADER_INIT_MEMBER(pci_bus_region, start)
PS_HEADER_INIT_MEMBER(pci_bus_region, end)
PS_HEADER_INIT_TYPE(pci_host_bridge)
PS_HEADER_INIT_MEMBER(pci_host_bridge, dev)
PS_HEADER_INIT_MEMBER(pci_host_bridge, bus)
PS_HEADER_INIT_MEMBER(pci_host_bridge, ops)
PS_HEADER_INIT_MEMBER(pci_host_bridge, sysdata)
PS_HEADER_INIT_MEMBER(pci_host_bridge, busnr)
PS_HEADER_INIT_MEMBER(pci_host_bridge, windows)
PS_HEADER_INIT_MEMBER(pci_host_bridge, swizzle_irq)
PS_HEADER_INIT_MEMBER(pci_host_bridge, map_irq)
PS_HEADER_INIT_MEMBER(pci_host_bridge, release_fn)
PS_HEADER_INIT_MEMBER(pci_host_bridge, release_data)
PS_HEADER_INIT_MEMBER(pci_host_bridge, msi)
PS_HEADER_INIT_MEMBER(pci_host_bridge, align_resource)
PS_HEADER_INIT_MEMBER(pci_host_bridge, private)
PS_HEADER_INIT_TYPE(pci_ops)
PS_HEADER_INIT_MEMBER(pci_ops, add_bus)
PS_HEADER_INIT_MEMBER(pci_ops, remove_bus)
PS_HEADER_INIT_MEMBER(pci_ops, map_bus)
PS_HEADER_INIT_MEMBER(pci_ops, read)
PS_HEADER_INIT_MEMBER(pci_ops, write)
PS_HEADER_INIT_TYPE(pci_cap_saved_state)
PS_HEADER_INIT_TYPE(device)
PS_HEADER_INIT_MEMBER(device, parent)
PS_HEADER_INIT_MEMBER(device, p)
PS_HEADER_INIT_MEMBER(device, kobj)
PS_HEADER_INIT_MEMBER(device, init_name)
PS_HEADER_INIT_MEMBER(device, type)
PS_HEADER_INIT_MEMBER(device, mutex)
PS_HEADER_INIT_MEMBER(device, bus)
PS_HEADER_INIT_MEMBER(device, driver)
PS_HEADER_INIT_MEMBER(device, platform_data)
PS_HEADER_INIT_MEMBER(device, driver_data)
PS_HEADER_INIT_MEMBER(device, links)
PS_HEADER_INIT_MEMBER(device, power)
PS_HEADER_INIT_MEMBER(device, pm_domain)
PS_HEADER_INIT_MEMBER(device, msi_domain)
PS_HEADER_INIT_MEMBER(device, msi_list)
PS_HEADER_INIT_MEMBER(device, numa_node)
PS_HEADER_INIT_MEMBER(device, dma_ops)
PS_HEADER_INIT_MEMBER(device, dma_mask)
PS_HEADER_INIT_MEMBER(device, coherent_dma_mask)
PS_HEADER_INIT_MEMBER(device, dma_pfn_offset)
PS_HEADER_INIT_MEMBER(device, dma_parms)
PS_HEADER_INIT_MEMBER(device, dma_pools)
PS_HEADER_INIT_MEMBER(device, dma_mem)
PS_HEADER_INIT_MEMBER(device, archdata)
PS_HEADER_INIT_MEMBER(device, of_node)
PS_HEADER_INIT_MEMBER(device, fwnode)
PS_HEADER_INIT_MEMBER(device, devt)
PS_HEADER_INIT_MEMBER(device, id)
PS_HEADER_INIT_MEMBER(device, devres_lock)
PS_HEADER_INIT_MEMBER(device, devres_head)
PS_HEADER_INIT_MEMBER(device, knode_class)
PS_HEADER_INIT_MEMBER(device, class)
PS_HEADER_INIT_MEMBER(device, groups)
PS_HEADER_INIT_MEMBER(device, release)
PS_HEADER_INIT_MEMBER(device, iommu_group)
PS_HEADER_INIT_MEMBER(device, iommu_fwspec)
PS_HEADER_INIT_TYPE(device_type)
PS_HEADER_INIT_MEMBER(device_type, name)
PS_HEADER_INIT_MEMBER(device_type, groups)
PS_HEADER_INIT_MEMBER(device_type, uevent)
PS_HEADER_INIT_MEMBER(device_type, devnode)
PS_HEADER_INIT_MEMBER(device_type, release)
PS_HEADER_INIT_MEMBER(device_type, pm)
PS_HEADER_INIT_TYPE(device_node)
PS_HEADER_INIT_MEMBER(device_node, name)
PS_HEADER_INIT_MEMBER(device_node, type)
PS_HEADER_INIT_MEMBER(device_node, phandle)
PS_HEADER_INIT_MEMBER(device_node, full_name)
PS_HEADER_INIT_MEMBER(device_node, fwnode)
PS_HEADER_INIT_MEMBER(device_node, properties)
PS_HEADER_INIT_MEMBER(device_node, deadprops)
PS_HEADER_INIT_MEMBER(device_node, parent)
PS_HEADER_INIT_MEMBER(device_node, child)
PS_HEADER_INIT_MEMBER(device_node, sibling)
PS_HEADER_INIT_MEMBER(device_node, _flags)
PS_HEADER_INIT_MEMBER(device_node, data)
PS_HEADER_INIT_TYPE(device_attribute)
PS_HEADER_INIT_MEMBER(device_attribute, attr)
PS_HEADER_INIT_MEMBER(device_attribute, store)
PS_HEADER_INIT_MEMBER(device_attribute, show)
PS_HEADER_INIT_TYPE(device_driver)
PS_HEADER_INIT_MEMBER(device_driver, name)
PS_HEADER_INIT_MEMBER(device_driver, bus)
PS_HEADER_INIT_MEMBER(device_driver, owner)
PS_HEADER_INIT_MEMBER(device_driver, mod_name)
PS_HEADER_INIT_MEMBER(device_driver, suppress_bind_attrs)
PS_HEADER_INIT_MEMBER(device_driver, probe_type)
PS_HEADER_INIT_MEMBER(device_driver, of_match_table)
PS_HEADER_INIT_MEMBER(device_driver, acpi_match_table)
PS_HEADER_INIT_MEMBER(device_driver, probe)
PS_HEADER_INIT_MEMBER(device_driver, remove)
PS_HEADER_INIT_MEMBER(device_driver, shutdown)
PS_HEADER_INIT_MEMBER(device_driver, suspend)
PS_HEADER_INIT_MEMBER(device_driver, resume)
PS_HEADER_INIT_MEMBER(device_driver, groups)
PS_HEADER_INIT_MEMBER(device_driver, pm)
PS_HEADER_INIT_MEMBER(device_driver, p)
PS_HEADER_INIT_TYPE(device_link)
PS_HEADER_INIT_MEMBER(device_link, supplier)
PS_HEADER_INIT_MEMBER(device_link, s_node)
PS_HEADER_INIT_MEMBER(device_link, consumer)
PS_HEADER_INIT_MEMBER(device_link, c_node)
PS_HEADER_INIT_MEMBER(device_link, status)
PS_HEADER_INIT_MEMBER(device_link, flags)
PS_HEADER_INIT_MEMBER(device_link, rpm_active)
PS_HEADER_INIT_TYPE(dev_links_info)
PS_HEADER_INIT_MEMBER(dev_links_info, suppliers)
PS_HEADER_INIT_MEMBER(dev_links_info, consumers)
PS_HEADER_INIT_MEMBER(dev_links_info, status)
PS_HEADER_INIT_TYPE(dev_ext_attribute)
PS_HEADER_INIT_MEMBER(dev_ext_attribute, attr)
PS_HEADER_INIT_MEMBER(dev_ext_attribute, var)
PS_HEADER_INIT_TYPE(class)
PS_HEADER_INIT_MEMBER(class, name)
PS_HEADER_INIT_MEMBER(class, owner)
PS_HEADER_INIT_MEMBER(class, class_groups)
PS_HEADER_INIT_MEMBER(class, dev_groups)
PS_HEADER_INIT_MEMBER(class, dev_kobj)
PS_HEADER_INIT_MEMBER(class, dev_uevent)
PS_HEADER_INIT_MEMBER(class, devnode)
PS_HEADER_INIT_MEMBER(class, class_release)
PS_HEADER_INIT_MEMBER(class, dev_release)
PS_HEADER_INIT_MEMBER(class, shutdown_pre)
PS_HEADER_INIT_MEMBER(class, ns_type)
PS_HEADER_INIT_MEMBER(class, namespace)
PS_HEADER_INIT_MEMBER(class, pm)
PS_HEADER_INIT_MEMBER(class, p)
PS_HEADER_INIT_TYPE(class_dev_iter)
PS_HEADER_INIT_MEMBER(class_dev_iter, ki)
PS_HEADER_INIT_MEMBER(class_dev_iter, type)
PS_HEADER_INIT_TYPE(class_interface)
PS_HEADER_INIT_MEMBER(class_interface, node)
PS_HEADER_INIT_MEMBER(class_interface, class)
PS_HEADER_INIT_MEMBER(class_interface, add_dev)
PS_HEADER_INIT_MEMBER(class_interface, remove_dev)
PS_HEADER_INIT_TYPE(class_attribute_string)
PS_HEADER_INIT_MEMBER(class_attribute_string, attr)
PS_HEADER_INIT_MEMBER(class_attribute_string, str)
PS_HEADER_INIT_TYPE(class_attribute)
PS_HEADER_INIT_MEMBER(class_attribute, attr)
PS_HEADER_INIT_MEMBER(class_attribute, show)
PS_HEADER_INIT_MEMBER(class_attribute, store)
PS_HEADER_INIT_TYPE(bus_type)
PS_HEADER_INIT_MEMBER(bus_type, name)
PS_HEADER_INIT_MEMBER(bus_type, dev_name)
PS_HEADER_INIT_MEMBER(bus_type, dev_root)
PS_HEADER_INIT_MEMBER(bus_type, bus_groups)
PS_HEADER_INIT_MEMBER(bus_type, dev_groups)
PS_HEADER_INIT_MEMBER(bus_type, drv_groups)
PS_HEADER_INIT_MEMBER(bus_type, match)
PS_HEADER_INIT_MEMBER(bus_type, uevent)
PS_HEADER_INIT_MEMBER(bus_type, probe)
PS_HEADER_INIT_MEMBER(bus_type, remove)
PS_HEADER_INIT_MEMBER(bus_type, shutdown)
PS_HEADER_INIT_MEMBER(bus_type, online)
PS_HEADER_INIT_MEMBER(bus_type, offline)
PS_HEADER_INIT_MEMBER(bus_type, suspend)
PS_HEADER_INIT_MEMBER(bus_type, resume)
PS_HEADER_INIT_MEMBER(bus_type, num_vf)
PS_HEADER_INIT_MEMBER(bus_type, pm)
PS_HEADER_INIT_MEMBER(bus_type, iommu_ops)
PS_HEADER_INIT_MEMBER(bus_type, p)
PS_HEADER_INIT_MEMBER(bus_type, lock_key)
PS_HEADER_INIT_TYPE(bus_attribute)
PS_HEADER_INIT_MEMBER(bus_attribute, attr)
PS_HEADER_INIT_MEMBER(bus_attribute, show)
PS_HEADER_INIT_MEMBER(bus_attribute, store)
PS_HEADER_INIT_TYPE(irq_domain)
PS_HEADER_INIT_TYPE(attribute_group)
PS_HEADER_INIT_MEMBER(attribute_group, name)
PS_HEADER_INIT_MEMBER(attribute_group, is_visible)
PS_HEADER_INIT_MEMBER(attribute_group, is_bin_visible)
PS_HEADER_INIT_MEMBER(attribute_group, attrs)
PS_HEADER_INIT_MEMBER(attribute_group, bin_attrs)
PS_HEADER_INIT_TYPE(bin_attribute)
PS_HEADER_INIT_MEMBER(bin_attribute, attr)
PS_HEADER_INIT_MEMBER(bin_attribute, size)
PS_HEADER_INIT_MEMBER(bin_attribute, private)
PS_HEADER_INIT_MEMBER(bin_attribute, read)
PS_HEADER_INIT_MEMBER(bin_attribute, mmap)
PS_HEADER_INIT_TYPE(sysfs_ops)
PS_HEADER_INIT_MEMBER(sysfs_ops, show)
PS_HEADER_INIT_MEMBER(sysfs_ops, store)
PS_HEADER_INIT_TYPE(cdev)
PS_HEADER_INIT_MEMBER(cdev, kobj)
PS_HEADER_INIT_MEMBER(cdev, opsj)
PS_HEADER_INIT_MEMBER(cdev, list)
PS_HEADER_INIT_MEMBER(cdev, dev)
PS_HEADER_INIT_MEMBER(cdev, count)
PS_HEADER_INIT_TYPE(dev_pm_ops)
PS_HEADER_INIT_MEMBER(dev_pm_ops, prepare)
PS_HEADER_INIT_MEMBER(dev_pm_ops, complete)
PS_HEADER_INIT_MEMBER(dev_pm_ops, suspend)
PS_HEADER_INIT_MEMBER(dev_pm_ops, resume)
PS_HEADER_INIT_MEMBER(dev_pm_ops, freeze)
PS_HEADER_INIT_MEMBER(dev_pm_ops, thaw)
PS_HEADER_INIT_MEMBER(dev_pm_ops, poweroff)
PS_HEADER_INIT_MEMBER(dev_pm_ops, restore)
PS_HEADER_INIT_MEMBER(dev_pm_ops, suspend_late)
PS_HEADER_INIT_MEMBER(dev_pm_ops, resume_early)
PS_HEADER_INIT_MEMBER(dev_pm_ops, freeze_late)
PS_HEADER_INIT_MEMBER(dev_pm_ops, thaw_early)
PS_HEADER_INIT_MEMBER(dev_pm_ops, poweroff_late)
PS_HEADER_INIT_MEMBER(dev_pm_ops, restore_early)
PS_HEADER_INIT_MEMBER(dev_pm_ops, suspend_noirq)
PS_HEADER_INIT_MEMBER(dev_pm_ops, resume_noirq)
PS_HEADER_INIT_MEMBER(dev_pm_ops, freeze_noirq)
PS_HEADER_INIT_MEMBER(dev_pm_ops, thaw_noirq)
PS_HEADER_INIT_MEMBER(dev_pm_ops, poweroff_noirq)
PS_HEADER_INIT_MEMBER(dev_pm_ops, restore_noirq)
PS_HEADER_INIT_MEMBER(dev_pm_ops, runtime_suspend)
PS_HEADER_INIT_MEMBER(dev_pm_ops, runtime_resume)
PS_HEADER_INIT_MEMBER(dev_pm_ops, runtime_idle)
PS_HEADER_INIT_TYPE(dev_pm_domain)
PS_HEADER_INIT_MEMBER(dev_pm_domain, ops)
PS_HEADER_INIT_MEMBER(dev_pm_domain, detach)
PS_HEADER_INIT_MEMBER(dev_pm_domain, activate)
PS_HEADER_INIT_MEMBER(dev_pm_domain, sync)
PS_HEADER_INIT_MEMBER(dev_pm_domain, dismiss)
PS_HEADER_INIT_TYPE(pm_subsys_data)
PS_HEADER_INIT_MEMBER(pm_subsys_data, lock)
PS_HEADER_INIT_MEMBER(pm_subsys_data, refcount)
PS_HEADER_INIT_TYPE(module)
PS_HEADER_INIT_TYPE(module_attribute)
PS_HEADER_INIT_TYPE(module_kobject)
PS_HEADER_INIT_TYPE(module_version_attribute)
PS_HEADER_INIT_TYPE(module_use)
PS_HEADER_INIT_TYPE(module_layout)
PS_HEADER_INIT_TYPE(mod_tree_node)
PS_HEADER_INIT_TYPE(mod_kallsyms)
PS_HEADER_INIT_TYPE(dma_map_ops)
PS_HEADER_INIT_MEMBER(dma_map_ops, alloc)
PS_HEADER_INIT_MEMBER(dma_map_ops, free)
PS_HEADER_INIT_MEMBER(dma_map_ops, mmap)
PS_HEADER_INIT_MEMBER(dma_map_ops, get_sgtable)
PS_HEADER_INIT_MEMBER(dma_map_ops, map_page)
PS_HEADER_INIT_MEMBER(dma_map_ops, unmap_page)
PS_HEADER_INIT_MEMBER(dma_map_ops, map_sg)
PS_HEADER_INIT_MEMBER(dma_map_ops, unmap_sg)
PS_HEADER_INIT_MEMBER(dma_map_ops, map_resource)
PS_HEADER_INIT_MEMBER(dma_map_ops, unmap_resource)
PS_HEADER_INIT_MEMBER(dma_map_ops, sync_single_for_cpu)
PS_HEADER_INIT_MEMBER(dma_map_ops, sync_single_for_device)
PS_HEADER_INIT_MEMBER(dma_map_ops, sync_sg_for_cpu)
PS_HEADER_INIT_MEMBER(dma_map_ops, sync_sg_for_device)
PS_HEADER_INIT_MEMBER(dma_map_ops, mapping_error)
PS_HEADER_INIT_MEMBER(dma_map_ops, dma_supported)
PS_HEADER_INIT_MEMBER(dma_map_ops, is_phys)
PS_HEADER_INIT_TYPE(qstr)
PS_HEADER_INIT_MEMBER(qstr, hash_len)
PS_HEADER_INIT_MEMBER(qstr, name)
PS_HEADER_INIT_TYPE(dentry)
PS_HEADER_INIT_MEMBER(dentry, d_flags)
PS_HEADER_INIT_MEMBER(dentry, d_seq)
PS_HEADER_INIT_MEMBER(dentry, d_hash)
PS_HEADER_INIT_MEMBER(dentry, d_parent)
PS_HEADER_INIT_MEMBER(dentry, d_name)
PS_HEADER_INIT_MEMBER(dentry, d_inode)
PS_HEADER_INIT_MEMBER(dentry, d_iname)
PS_HEADER_INIT_MEMBER(dentry, d_lockref)
PS_HEADER_INIT_MEMBER(dentry, d_op)
PS_HEADER_INIT_MEMBER(dentry, d_sb)
PS_HEADER_INIT_MEMBER(dentry, d_time)
PS_HEADER_INIT_MEMBER(dentry, d_fsdata)
PS_HEADER_INIT_MEMBER(dentry, d_lru)
PS_HEADER_INIT_MEMBER(dentry, d_wait)
PS_HEADER_INIT_MEMBER(dentry, d_child)
PS_HEADER_INIT_MEMBER(dentry, d_subdirs)
PS_HEADER_INIT_MEMBER(dentry, d_u_d_alias)
PS_HEADER_INIT_MEMBER(dentry, d_u_generic)
PS_HEADER_INIT_TYPE(dentry_operations)
PS_HEADER_INIT_MEMBER(dentry_operations, d_revalidate)
PS_HEADER_INIT_MEMBER(dentry_operations, d_weak_revalidate)
PS_HEADER_INIT_MEMBER(dentry_operations, d_hash)
PS_HEADER_INIT_MEMBER(dentry_operations, d_compare)
PS_HEADER_INIT_MEMBER(dentry_operations, d_delete)
PS_HEADER_INIT_MEMBER(dentry_operations, d_init)
PS_HEADER_INIT_MEMBER(dentry_operations, d_release)
PS_HEADER_INIT_MEMBER(dentry_operations, d_prune)
PS_HEADER_INIT_MEMBER(dentry_operations, d_iput)
PS_HEADER_INIT_MEMBER(dentry_operations, d_dname)
PS_HEADER_INIT_MEMBER(dentry_operations, d_automount)
PS_HEADER_INIT_MEMBER(dentry_operations, d_manage)
PS_HEADER_INIT_MEMBER(dentry_operations, d_real)
PS_HEADER_INIT_TYPE(dentry_stat)
PS_HEADER_INIT_MEMBER(dentry_stat, nr_dentry)
PS_HEADER_INIT_MEMBER(dentry_stat, nr_unused)
PS_HEADER_INIT_MEMBER(dentry_stat, age_limit)
PS_HEADER_INIT_MEMBER(dentry_stat, want_pages)
PS_HEADER_INIT_MEMBER(dentry_stat, dummy)
PS_HEADER_INIT_TYPE(wait_queue_entry)
PS_HEADER_INIT_MEMBER(wait_queue_entry, flags)
PS_HEADER_INIT_MEMBER(wait_queue_entry, private)
PS_HEADER_INIT_MEMBER(wait_queue_entry, entry)
PS_HEADER_INIT_MEMBER(wait_queue_entry, func)
#endif
